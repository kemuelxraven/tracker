<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maribank · Budget Tracker</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;0,800;1,400;1,600&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;1,9..40,300&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --gold-50: #fffbf0;
            --gold-100: #fff3c4;
            --gold-200: #fde68a;
            --gold-300: #fbbf24;
            --gold-400: #f59e0b;
            --gold-500: #d97706;
            --gold-600: #b45309;
            --gold-800: #78350f;
            --gold-900: #451a03;
            --green-400: #4ade80;
            --green-600: #16a34a;
            --cream: #fffdf5;
            --bark: #3d2007;
            --bark-light: #5c320f;
            --danger: #dc2626;
            --loan-color: #7c3aed;
            --loan-bg: #f5f3ff;
            --loan-border: #ddd6fe;
            --radius-card: 22px;
            --radius-btn: 12px;
            --radius-input: 10px;
            --font-display: 'Playfair Display', Georgia, serif;
            --font-body: 'DM Sans', system-ui, sans-serif;
            --transition: 0.22s cubic-bezier(0.4, 0, 0.2, 1);
        }

        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box
        }

        html {
            scroll-behavior: smooth
        }

        body {
            font-family: var(--font-body);
            background: var(--cream);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative
        }

        body::before {
            content: '';
            position: fixed;
            inset: 0;
            background:
                radial-gradient(ellipse 70% 50% at 10% 5%, rgba(253, 230, 138, .28) 0%, transparent 65%),
                radial-gradient(ellipse 50% 60% at 90% 90%, rgba(180, 83, 9, .12) 0%, transparent 65%);
            pointer-events: none;
            z-index: 0;
            animation: bgShift 12s ease-in-out infinite alternate;
        }

        @keyframes bgShift {
            0% {
                background-position: 0% 0%
            }

            100% {
                background-position: 5% 3%
            }
        }

        .particle {
            position: fixed;
            border-radius: 50%;
            pointer-events: none;
            z-index: 0;
            background: radial-gradient(circle, rgba(253, 230, 138, .25) 0%, transparent 70%);
            animation: floatParticle linear infinite;
        }

        @keyframes floatParticle {
            0% {
                transform: translateY(110vh) rotate(0deg) scale(1)
            }

            50% {
                transform: translateY(50vh) rotate(180deg) scale(1.2)
            }

            100% {
                transform: translateY(-10vh) rotate(360deg) scale(0.8)
            }
        }

        .header {
            position: sticky;
            top: 0;
            z-index: 100;
            background: linear-gradient(135deg, var(--bark) 0%, #7c4a1a 100%);
            border-bottom: 1px solid rgba(253, 230, 138, .25);
            box-shadow: 0 2px 16px rgba(61, 32, 7, .3), inset 0 1px 0 rgba(255, 243, 196, .12);
            padding: 0 20px;
            overflow: hidden;
        }

        .header::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent 0%, rgba(253, 230, 138, .06) 40%, rgba(245, 158, 11, .1) 60%, transparent 100%);
            animation: headerShimmer 4s ease-in-out infinite;
        }

        @keyframes headerShimmer {

            0%,
            100% {
                transform: translateX(-100%)
            }

            50% {
                transform: translateX(100%)
            }
        }

        .header-inner {
            max-width: 680px;
            margin: 0 auto;
            height: 68px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 16px;
            position: relative;
            z-index: 1
        }

        .header-brand {
            display: flex;
            align-items: center;
            gap: 12px
        }

        .header-sunflower {
            width: 42px;
            height: 42px;
            background: linear-gradient(135deg, var(--gold-200), var(--gold-400), var(--gold-300));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 22px;
            box-shadow: 0 0 0 3px rgba(253, 230, 138, .25), 0 4px 20px rgba(180, 83, 9, .4), 0 0 40px rgba(245, 158, 11, .2);
            animation: sunflowerPulse 3s ease-in-out infinite;
            flex-shrink: 0;
        }

        @keyframes sunflowerPulse {

            0%,
            100% {
                box-shadow: 0 0 0 3px rgba(253, 230, 138, .25), 0 4px 20px rgba(180, 83, 9, .4), 0 0 40px rgba(245, 158, 11, .2);
                transform: rotate(0deg)
            }

            33% {
                box-shadow: 0 0 0 6px rgba(253, 230, 138, .18), 0 4px 28px rgba(245, 158, 11, .5), 0 0 60px rgba(245, 158, 11, .3);
                transform: rotate(5deg)
            }

            66% {
                box-shadow: 0 0 0 3px rgba(253, 230, 138, .25), 0 4px 20px rgba(180, 83, 9, .4), 0 0 40px rgba(245, 158, 11, .2);
                transform: rotate(-3deg)
            }
        }

        .header-title {
            font-family: var(--font-display);
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--gold-100);
            letter-spacing: .2px;
            line-height: 1.1
        }

        .header-subtitle {
            font-size: .62rem;
            font-weight: 300;
            color: rgba(253, 230, 138, .5);
            letter-spacing: 2.5px;
            text-transform: uppercase;
            margin-top: 3px;
            display: block;
            font-style: italic
        }

        .header-btns {
            display: flex;
            gap: 8px;
            align-items: center;
            flex-shrink: 0
        }

        .btn-report {
            background: linear-gradient(135deg, rgba(245, 158, 11, .5), rgba(253, 230, 138, .35));
            border: 1.5px solid rgba(253, 230, 138, .7);
            color: #000;
            border-radius: 30px;
            padding: 8px 18px;
            font-family: var(--font-body);
            font-size: .8rem;
            font-weight: 500;
            cursor: pointer;
            letter-spacing: .3px;
            transition: var(--transition);
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 6px;
            position: relative;
            overflow: hidden;
        }

        .btn-report::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .2), transparent);
            transform: translateX(-100%);
            transition: .5s
        }

        .btn-report:hover::before {
            transform: translateX(100%)
        }

        .btn-report:hover {
            background: linear-gradient(135deg, rgba(245, 158, 11, .7), rgba(253, 230, 138, .5));
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(180, 83, 9, .35)
        }

        .btn-logs {
            background: linear-gradient(135deg, rgba(124, 58, 237, .5), rgba(167, 139, 250, .35));
            border: 1.5px solid rgba(167, 139, 250, .75);
            color: #000;
            border-radius: 30px;
            padding: 8px 18px;
            font-family: var(--font-body);
            font-size: .8rem;
            font-weight: 500;
            cursor: pointer;
            letter-spacing: .3px;
            transition: var(--transition);
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 6px;
            position: relative;
            overflow: hidden;
        }

        .btn-logs::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .2), transparent);
            transform: translateX(-100%);
            transition: .5s
        }

        .btn-logs:hover::before {
            transform: translateX(100%)
        }

        .btn-logs:hover {
            background: linear-gradient(135deg, rgba(124, 58, 237, .7), rgba(167, 139, 250, .5));
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(124, 58, 237, .35)
        }

        .container {
            max-width: 680px;
            margin: 0 auto;
            padding: 28px 20px 100px;
            position: relative;
            z-index: 1
        }

        .budget-card {
            border-radius: var(--radius-card);
            padding: 32px;
            margin-bottom: 28px;
            position: relative;
            overflow: hidden;
            background: linear-gradient(145deg, var(--bark) 0%, #5c2b0c 55%, #7c4a1a 100%);
            box-shadow: 0 16px 40px rgba(61, 32, 7, .25), inset 0 1px 0 rgba(255, 243, 196, .1);
            animation: slideDown .6s cubic-bezier(.16, 1, .3, 1) both;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-30px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        .budget-card::before {
            content: '';
            position: absolute;
            inset: 0;
            background:
                radial-gradient(ellipse 80% 60% at 75% 25%, rgba(253, 230, 138, .1) 0%, transparent 60%);
            animation: cardMeshMove 8s ease-in-out infinite alternate;
            pointer-events: none;
        }

        @keyframes cardMeshMove {
            0% {
                transform: scale(1) translate(0, 0)
            }

            100% {
                transform: scale(1.08) translate(-2%, 1%)
            }
        }

        .budget-card-deco {
            position: absolute;
            right: -40px;
            top: -40px;
            width: 240px;
            height: 240px;
            background: radial-gradient(circle, rgba(253, 230, 138, .15) 0%, rgba(245, 158, 11, .06) 40%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            animation: decoFloat 10s ease-in-out infinite;
        }

        @keyframes decoFloat {

            0%,
            100% {
                transform: translate(0, 0) scale(1)
            }

            50% {
                transform: translate(-15px, 10px) scale(1.1)
            }
        }

        .budget-card-sun {
            position: absolute;
            right: 28px;
            top: 20px;
            font-size: 5rem;
            opacity: .06;
            pointer-events: none;
            animation: sunSpin 25s linear infinite;
        }

        @keyframes sunSpin {
            from {
                transform: rotate(15deg)
            }

            to {
                transform: rotate(375deg)
            }
        }

        .budget-eyebrow {
            font-size: .63rem;
            font-weight: 500;
            letter-spacing: 2.8px;
            text-transform: uppercase;
            color: rgba(253, 230, 138, .5);
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 8px;
            position: relative;
            z-index: 1
        }

        .budget-eyebrow::before {
            content: '';
            display: inline-block;
            width: 24px;
            height: 1px;
            background: linear-gradient(90deg, rgba(253, 230, 138, .5), transparent)
        }

        /* ── ADD MONEY BUTTON (above budget display) ── */
        .btn-add-money {
            background: linear-gradient(135deg, rgba(245, 158, 11, .35), rgba(180, 83, 9, .45));
            border: 1.5px solid rgba(253, 230, 138, .6);
            color: var(--gold-100);
            border-radius: 30px;
            padding: 7px 16px;
            font-family: var(--font-body);
            font-size: .78rem;
            font-weight: 600;
            cursor: pointer;
            letter-spacing: .3px;
            transition: var(--transition);
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 5px;
            position: relative;
            overflow: hidden;
            flex-shrink: 0;
            margin-top: 12px;
        }

        .btn-add-money::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(253, 230, 138, .15), transparent);
            transform: translateX(-100%);
            transition: .5s
        }

        .btn-add-money:hover::before {
            transform: translateX(100%)
        }

        .btn-add-money:hover {
            background: linear-gradient(135deg, rgba(245, 158, 11, .55), rgba(180, 83, 9, .65));
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(180, 83, 9, .35)
        }

        .budget-amount-row {
            display: flex;
            align-items: center;
            gap: 12px;
            flex-wrap: wrap;
            position: relative;
            z-index: 1
        }

        /* Budget amount is now display-only */
        .budget-amount {
            font-family: var(--font-display);
            font-size: clamp(2rem, 7vw, 3rem);
            font-weight: 800;
            color: transparent;
            background: linear-gradient(135deg, var(--gold-100) 0%, var(--gold-200) 40%, var(--gold-300) 70%, var(--gold-100) 100%);
            -webkit-background-clip: text;
            background-clip: text;
            padding-bottom: 3px;
            flex: 1;
            min-width: 0;
            word-break: break-all;
            line-height: 1.1;
            letter-spacing: -.5px;
            animation: goldShimmer 3s ease-in-out infinite;
            background-size: 200% auto;
        }

        @keyframes goldShimmer {

            0%,
            100% {
                background-position: 0% 50%
            }

            50% {
                background-position: 100% 50%
            }
        }

        .stats-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-top: 22px;
            position: relative;
            z-index: 1
        }

        .stat-pill {
            background: rgba(255, 255, 255, .06);
            border: 1px solid rgba(255, 255, 255, .1);
            border-radius: 16px;
            padding: 14px 16px;
            backdrop-filter: blur(12px);
            transition: all var(--transition);
            position: relative;
            overflow: hidden;
        }

        .stat-pill::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(255, 255, 255, .04) 0%, transparent 100%);
            opacity: 0;
            transition: opacity var(--transition);
        }

        .stat-pill:hover::before {
            opacity: 1
        }

        .stat-pill:hover {
            background: rgba(255, 255, 255, .1);
            border-color: rgba(255, 255, 255, .15);
            transform: translateY(-2px)
        }

        .stat-pill.green-tint {
            background: rgba(74, 222, 128, .09);
            border-color: rgba(74, 222, 128, .25)
        }

        .stat-pill.red-tint {
            background: rgba(220, 38, 38, .12);
            border-color: rgba(220, 38, 38, .25)
        }

        .stat-label {
            font-size: .58rem;
            font-weight: 500;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: rgba(253, 230, 138, .45);
            display: block;
            margin-bottom: 6px
        }

        .stat-value {
            font-family: var(--font-display);
            font-size: 1.1rem;
            font-weight: 700;
            display: block;
            letter-spacing: -.3px
        }

        .stat-value.gold {
            color: var(--gold-200)
        }

        .stat-value.green {
            color: #86efac
        }

        .stat-value.red {
            color: #fca5a5
        }

        .stat-value.yellow {
            color: var(--gold-200)
        }

        .stat-hint {
            font-size: .57rem;
            color: rgba(253, 230, 138, .28);
            display: block;
            margin-top: 4px;
            line-height: 1.4
        }

        .progress-section {
            margin-top: 20px;
            position: relative;
            z-index: 1
        }

        .progress-track {
            height: 7px;
            background: rgba(255, 255, 255, .08);
            border-radius: 99px;
            overflow: hidden;
            box-shadow: inset 0 2px 4px rgba(0, 0, 0, .2);
        }

        .progress-bar {
            height: 100%;
            border-radius: 99px;
            background: linear-gradient(90deg, #86efac 0%, #fbbf24 50%, #fde68a 85%, #fff3c4 100%);
            transition: width .8s cubic-bezier(.4, 0, .2, 1);
            position: relative;
            box-shadow: 0 0 12px rgba(251, 191, 36, .4), 0 2px 6px rgba(74, 222, 128, .3);
        }

        .progress-bar::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent 0%, rgba(255, 255, 255, .45) 50%, transparent 100%);
            animation: shimmer 2.5s ease-in-out infinite;
            border-radius: inherit;
        }

        @keyframes shimmer {
            0% {
                transform: translateX(-100%)
            }

            100% {
                transform: translateX(200%)
            }
        }

        .progress-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 7px
        }

        .progress-label {
            font-size: .63rem;
            color: rgba(253, 230, 138, .4);
            letter-spacing: .5px
        }

        .action-row {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin: 22px auto 0;
            max-width: 680px;
            padding: 0 20px
        }

        .dropdown-wrap {
            position: relative;
            flex-shrink: 0
        }

        .btn-data-menu {
            background: linear-gradient(135deg, rgba(245, 158, 11, .5), rgba(253, 230, 138, .35));
            border: 1.5px solid rgba(253, 230, 138, .7);
            color: #000;
            border-radius: 30px;
            padding: 8px 16px;
            font-family: var(--font-body);
            font-size: .8rem;
            font-weight: 500;
            cursor: pointer;
            letter-spacing: .3px;
            transition: var(--transition);
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 6px;
            position: relative;
            overflow: hidden;
        }

        .btn-data-menu::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .2), transparent);
            transform: translateX(-100%);
            transition: .5s
        }

        .btn-data-menu:hover::before {
            transform: translateX(100%)
        }

        .btn-data-menu:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(180, 83, 9, .3);
            background: linear-gradient(135deg, rgba(245, 158, 11, .7), rgba(253, 230, 138, .5))
        }

        .data-menu {
            display: none;
            position: absolute;
            top: calc(100% + 8px);
            right: 0;
            background: white;
            border: 1.5px solid #f0e4c0;
            border-radius: 16px;
            box-shadow: 0 12px 40px rgba(61, 32, 7, .2), 0 4px 12px rgba(61, 32, 7, .1);
            overflow: hidden;
            min-width: 200px;
            z-index: 300;
            animation: menuDrop .25s cubic-bezier(.4, 0, .2, 1) both;
        }

        @keyframes menuDrop {
            from {
                opacity: 0;
                transform: translateY(-10px) scale(.96)
            }

            to {
                opacity: 1;
                transform: translateY(0) scale(1)
            }
        }

        .data-menu.open {
            display: block
        }

        .data-menu-item {
            width: 100%;
            background: none;
            border: none;
            padding: 13px 18px;
            font-family: var(--font-body);
            font-size: .85rem;
            font-weight: 500;
            color: var(--bark);
            cursor: pointer;
            text-align: left;
            transition: all var(--transition);
            display: flex;
            align-items: center;
            gap: 9px;
            border-bottom: 1px solid #f5ead0;
        }

        .data-menu-item:last-child {
            border-bottom: none
        }

        .data-menu-item:hover {
            background: linear-gradient(90deg, #fff9ec, #fffbf5);
            padding-left: 22px
        }

        .data-menu-item.danger-item {
            color: #991b1b
        }

        .data-menu-item.danger-item:hover {
            background: linear-gradient(90deg, #fff1f2, #fff5f5)
        }

        .section-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 18px;
            gap: 12px;
            margin-top: 24px
        }

        .section-title {
            font-family: var(--font-display);
            font-size: 1.15rem;
            font-weight: 700;
            color: var(--bark);
            display: flex;
            align-items: center;
            gap: 10px;
            letter-spacing: -.2px;
        }

        .section-title::before {
            content: '';
            display: inline-block;
            width: 4px;
            height: 22px;
            background: linear-gradient(180deg, var(--gold-300), var(--gold-600));
            border-radius: 4px;
            box-shadow: 0 0 8px rgba(245, 158, 11, .4);
        }

        .btn-add-cat {
            background: linear-gradient(135deg, var(--green-600), #15803d);
            color: #dcfce7;
            border: none;
            border-radius: 30px;
            padding: 9px 20px;
            font-family: var(--font-body);
            font-size: .8rem;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 5px;
            transition: var(--transition);
            white-space: nowrap;
            flex-shrink: 0;
            box-shadow: 0 4px 16px rgba(22, 163, 74, .25);
            position: relative;
            overflow: hidden;
        }

        .btn-add-cat::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .2), transparent);
            transform: translateX(-100%);
            transition: .5s
        }

        .btn-add-cat:hover::before {
            transform: translateX(100%)
        }

        .btn-add-cat:hover {
            filter: brightness(1.12);
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(22, 163, 74, .4)
        }

        .add-cat-form {
            background: linear-gradient(145deg, #fff9ec, #fffbf5, #fff9ee);
            border: 1.5px solid var(--gold-200);
            border-radius: 20px;
            padding: 20px;
            margin-bottom: 18px;
            display: none;
            box-shadow: 0 8px 30px rgba(180, 83, 9, .1), inset 0 1px 0 rgba(253, 230, 138, .3);
            animation: formOpen .35s cubic-bezier(.4, 0, .2, 1) both;
        }

        @keyframes formOpen {
            from {
                opacity: 0;
                transform: translateY(-12px) scale(.97)
            }

            to {
                opacity: 1;
                transform: translateY(0) scale(1)
            }
        }

        .add-cat-form.open {
            display: block
        }

        .form-row {
            display: flex;
            gap: 8px;
            flex-wrap: wrap
        }

        .form-row input {
            flex: 1;
            min-width: 100px;
            border: 1.5px solid #e8d5a0;
            border-radius: var(--radius-input);
            padding: 10px 14px;
            font-family: var(--font-body);
            font-size: .88rem;
            background: white;
            outline: none;
            color: var(--bark);
            transition: all var(--transition);
        }

        .form-row input:focus {
            border-color: var(--gold-400);
            box-shadow: 0 0 0 4px rgba(245, 158, 11, .12), 0 4px 12px rgba(245, 158, 11, .08)
        }

        .btn-form-add {
            background: linear-gradient(135deg, var(--gold-500), var(--gold-600));
            color: white;
            border: none;
            border-radius: var(--radius-input);
            padding: 10px 20px;
            font-weight: 700;
            cursor: pointer;
            font-size: .86rem;
            white-space: nowrap;
            transition: var(--transition);
            box-shadow: 0 4px 12px rgba(180, 83, 9, .25);
        }

        .btn-form-add:hover {
            filter: brightness(1.12);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(180, 83, 9, .35)
        }

        .btn-form-cancel {
            background: #f0e4c0;
            color: var(--bark);
            border: none;
            border-radius: var(--radius-input);
            padding: 10px 16px;
            font-weight: 500;
            cursor: pointer;
            font-size: .86rem;
            transition: var(--transition);
        }

        .btn-form-cancel:hover {
            background: #e8d5a0;
            transform: translateY(-1px)
        }

        .categories {
            display: flex;
            flex-direction: column;
            gap: 16px
        }

        .cat-card {
            background: white;
            border-radius: var(--radius-card);
            border: 1.5px solid #f0e4c0;
            box-shadow: 0 2px 20px rgba(180, 83, 9, .07), 0 1px 4px rgba(61, 32, 7, .04);
            overflow: hidden;
            transition: all var(--transition);
            animation: cardIn .5s cubic-bezier(.16, 1, .3, 1) both;
            position: relative;
        }

        .cat-card::after {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: inherit;
            background: linear-gradient(135deg, rgba(253, 230, 138, .0) 0%, rgba(245, 158, 11, .0) 100%);
            transition: background var(--transition);
            pointer-events: none;
        }

        .cat-card:hover::after {
            background: linear-gradient(135deg, rgba(253, 230, 138, .03) 0%, rgba(245, 158, 11, .02) 100%)
        }

        @keyframes cardIn {
            from {
                opacity: 0;
                transform: translateY(16px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        .cat-card:hover {
            box-shadow: 0 12px 40px rgba(180, 83, 9, .15), 0 4px 12px rgba(61, 32, 7, .06);
            transform: translateY(-2px);
            border-color: #e0c87a
        }

        .cat-card.depleted {
            border-color: #fecaca
        }

        .cat-header {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 16px 20px 11px;
            cursor: pointer;
            user-select: none
        }

        .cat-icon {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, var(--gold-100), var(--gold-200));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.15rem;
            flex-shrink: 0;
            border: 1px solid var(--gold-200);
            box-shadow: 0 2px 8px rgba(180, 83, 9, .1);
            transition: transform var(--transition);
        }

        .cat-header:hover .cat-icon {
            transform: scale(1.1) rotate(5deg)
        }

        .cat-info {
            flex: 1;
            min-width: 0
        }

        .cat-name {
            font-weight: 600;
            font-size: .95rem;
            color: var(--bark);
            display: flex;
            align-items: center;
            gap: 7px;
            flex-wrap: wrap
        }

        .cat-sub {
            font-size: .7rem;
            color: #a08040;
            margin-top: 2px
        }

        .cat-amounts {
            text-align: right;
            flex-shrink: 0
        }

        .cat-remaining {
            font-family: var(--font-display);
            font-size: 1.05rem;
            font-weight: 700;
            color: var(--green-600);
            display: block;
            letter-spacing: -.3px
        }

        .cat-remaining.warning {
            color: var(--gold-500)
        }

        .cat-remaining.danger {
            color: var(--danger)
        }

        .cat-remaining.zero {
            color: #a08040
        }

        .cat-spent-sub {
            font-size: .65rem;
            color: #a08040;
            display: block;
            margin-top: 2px
        }

        .chevron {
            color: #c0a060;
            font-size: .7rem;
            transition: transform .35s cubic-bezier(.4, 0, .2, 1);
            flex-shrink: 0
        }

        .chevron.up {
            transform: rotate(180deg)
        }

        .cat-progress-wrap {
            padding: 0 20px;
            margin-bottom: 2px
        }

        .cat-progress-track {
            height: 5px;
            background: #f0e4c0;
            border-radius: 99px;
            overflow: hidden;
            box-shadow: inset 0 1px 3px rgba(180, 83, 9, .1)
        }

        .cat-progress-fill {
            height: 100%;
            border-radius: 99px;
            background: linear-gradient(90deg, #4ade80, #16a34a);
            transition: width .6s cubic-bezier(.4, 0, .2, 1);
            position: relative;
        }

        .cat-progress-fill::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .4), transparent);
            animation: catShimmer 2s ease-in-out infinite;
            border-radius: inherit;
        }

        @keyframes catShimmer {
            0% {
                transform: translateX(-200%)
            }

            100% {
                transform: translateX(300%)
            }
        }

        .cat-progress-fill.warning {
            background: linear-gradient(90deg, var(--gold-300), var(--gold-500))
        }

        .cat-progress-fill.danger {
            background: linear-gradient(90deg, #f87171, #dc2626)
        }

        .cat-body {
            padding: 11px 20px 18px;
            display: none
        }

        .cat-body.open {
            display: block
        }

        .depleted-notice {
            background: linear-gradient(135deg, #fff1f2, #fff5f5);
            border: 1px solid #fecaca;
            border-radius: 12px;
            padding: 11px 15px;
            font-size: .78rem;
            color: #991b1b;
            margin-bottom: 11px;
            display: none;
            align-items: center;
            gap: 8px;
            line-height: 1.4;
        }

        .log-type-row {
            display: flex;
            gap: 6px;
            margin-bottom: 10px
        }

        .log-type-btn {
            flex: 1;
            padding: 8px 6px;
            border: 1.5px solid #e8d5a0;
            border-radius: 10px;
            background: white;
            font-family: var(--font-body);
            font-size: .76rem;
            font-weight: 600;
            cursor: pointer;
            color: #a08040;
            transition: all var(--transition);
            text-align: center;
        }

        .log-type-btn.active-spent {
            background: linear-gradient(135deg, #fff7ed, #fef3c7);
            border-color: var(--gold-400);
            color: var(--gold-600);
            box-shadow: 0 3px 10px rgba(245, 158, 11, .2);
        }

        .log-type-btn.active-loan {
            background: linear-gradient(135deg, #f5f3ff, #ede9fe);
            border-color: var(--loan-color);
            color: var(--loan-color);
            box-shadow: 0 3px 10px rgba(124, 58, 237, .15);
        }

        .exp-input-row {
            display: flex;
            gap: 6px;
            margin-bottom: 11px;
            flex-wrap: wrap
        }

        .exp-input-row input {
            flex: 1;
            min-width: 80px;
            border: 1.5px solid #e8d5a0;
            border-radius: 10px;
            padding: 9px 11px;
            font-family: var(--font-body);
            font-size: .83rem;
            color: var(--bark);
            background: var(--cream);
            outline: none;
            transition: all var(--transition);
        }

        .exp-input-row input:focus {
            border-color: var(--gold-400);
            box-shadow: 0 0 0 3px rgba(245, 158, 11, .1), 0 4px 10px rgba(245, 158, 11, .06)
        }

        .exp-input-row input:disabled {
            opacity: .4;
            cursor: not-allowed
        }

        .btn-log {
            background: linear-gradient(135deg, var(--bark), #5c2b0c, var(--bark-light));
            color: var(--gold-200);
            border: none;
            border-radius: 10px;
            padding: 9px 15px;
            font-size: .83rem;
            font-weight: 700;
            cursor: pointer;
            white-space: nowrap;
            transition: var(--transition);
            letter-spacing: .2px;
            box-shadow: 0 4px 12px rgba(61, 32, 7, .25);
            position: relative;
            overflow: hidden;
        }

        .btn-log::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, transparent, rgba(253, 230, 138, .1), transparent);
            transform: translateX(-100%);
            transition: .5s
        }

        .btn-log:hover::before {
            transform: translateX(100%)
        }

        .btn-log:hover:not(:disabled) {
            filter: brightness(1.2);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(61, 32, 7, .35)
        }

        .btn-log:disabled {
            opacity: .4;
            cursor: not-allowed
        }

        .expense-log {
            display: flex;
            flex-direction: column;
            gap: 7px;
            max-height: 280px;
            overflow-y: auto;
            padding-right: 2px
        }

        .expense-log::-webkit-scrollbar {
            width: 3px
        }

        .expense-log::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--gold-300), var(--gold-500));
            border-radius: 3px
        }

        .expense-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 13px;
            background: linear-gradient(135deg, var(--cream), #fff9ec, #fffdf5);
            border-radius: 12px;
            border: 1px solid #f0e4c0;
            animation: itemIn .35s cubic-bezier(.16, 1, .3, 1) both;
            transition: all var(--transition);
        }

        .expense-item:hover {
            background: linear-gradient(135deg, #fff9ec, #fef3c7);
            border-color: #e8d5a0;
            transform: translateX(3px)
        }

        .expense-item.loan-item {
            background: linear-gradient(135deg, #faf5ff, #f5f3ff, #fdfaff);
            border-color: var(--loan-border)
        }

        .expense-item.loan-item:hover {
            background: linear-gradient(135deg, #f5f3ff, #ede9fe);
            border-color: #c4b5fd
        }

        @keyframes itemIn {
            from {
                opacity: 0;
                transform: translateX(-12px)
            }

            to {
                opacity: 1;
                transform: translateX(0)
            }
        }

        .exp-main {
            flex: 1;
            min-width: 0
        }

        .exp-note {
            font-size: .83rem;
            color: var(--bark);
            font-weight: 500;
            display: block;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap
        }

        .exp-meta {
            font-size: .63rem;
            color: #a08040;
            display: block;
            margin-top: 2px
        }

        .exp-amount {
            font-weight: 700;
            color: var(--bark);
            font-size: .9rem;
            white-space: nowrap;
            flex-shrink: 0;
            font-family: var(--font-display);
            letter-spacing: -.2px
        }

        .exp-amount.loan-amt {
            color: var(--loan-color)
        }

        .type-chip {
            font-size: .56rem;
            font-weight: 700;
            letter-spacing: .6px;
            padding: 3px 7px;
            border-radius: 6px;
            text-transform: uppercase;
            flex-shrink: 0
        }

        .chip-spent {
            background: #fef3c7;
            color: var(--gold-600)
        }

        .chip-loan {
            background: #ede9fe;
            color: var(--loan-color)
        }

        .btn-del-exp,
        .btn-edit-exp {
            background: none;
            border: none;
            cursor: pointer;
            padding: 0;
            line-height: 1;
            flex-shrink: 0;
            transition: all var(--transition);
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 6px;
        }

        .btn-del-exp {
            color: #c0a060;
            font-size: 1.1rem
        }

        .btn-del-exp:hover {
            color: var(--danger);
            background: #fee2e2;
            transform: scale(1.2)
        }

        .btn-edit-exp {
            color: #c0a060;
            font-size: .95rem
        }

        .btn-edit-exp:hover {
            color: var(--gold-500);
            background: var(--gold-100);
            transform: scale(1.2)
        }

        .no-expenses {
            font-size: .78rem;
            color: #c0a060;
            text-align: center;
            padding: 14px 0;
            font-style: italic
        }

        .cat-actions {
            display: flex;
            gap: 8px;
            margin-top: 13px;
            padding-top: 13px;
            border-top: 1px solid #f0e4c0
        }

        .btn-edit-cat,
        .btn-del-cat {
            border: none;
            border-radius: 9px;
            padding: 7px 14px;
            font-size: .73rem;
            cursor: pointer;
            font-weight: 600;
            transition: all var(--transition)
        }

        .btn-edit-cat {
            background: linear-gradient(135deg, #f0e4c0, #edd9a3);
            color: var(--bark)
        }

        .btn-edit-cat:hover {
            background: linear-gradient(135deg, #e8d5a0, #e0c87a);
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(180, 83, 9, .12)
        }

        .btn-del-cat {
            background: linear-gradient(135deg, #fee2e2, #fecaca);
            color: #991b1b
        }

        .btn-del-cat:hover {
            background: linear-gradient(135deg, #fecaca, #fca5a5);
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(220, 38, 38, .15)
        }

        .badge-over {
            background: linear-gradient(135deg, #fee2e2, #fecaca);
            color: #991b1b;
            font-size: .58rem;
            font-weight: 700;
            letter-spacing: .6px;
            padding: 2px 8px;
            border-radius: 6px;
            text-transform: uppercase;
            border: 1px solid #fca5a5
        }

        .badge-zero {
            background: linear-gradient(135deg, #fef3c7, #fde68a);
            color: #92400e;
            font-size: .58rem;
            font-weight: 700;
            letter-spacing: .6px;
            padding: 2px 8px;
            border-radius: 6px;
            text-transform: uppercase;
            border: 1px solid #fde68a
        }

        .modal-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(61, 32, 7, .6);
            backdrop-filter: blur(8px);
            z-index: 200;
            align-items: center;
            justify-content: center;
            padding: 20px
        }

        .modal-overlay.open {
            display: flex
        }

        .modal {
            background: white;
            border-radius: var(--radius-card);
            padding: 30px;
            width: 100%;
            max-width: 390px;
            box-shadow: 0 30px 80px rgba(61, 32, 7, .35), 0 8px 24px rgba(61, 32, 7, .15);
            animation: modalIn .38s cubic-bezier(.16, 1, .3, 1) both;
            border: 1px solid rgba(253, 230, 138, .15);
        }

        @keyframes modalIn {
            from {
                opacity: 0;
                transform: scale(.88) translateY(24px)
            }

            to {
                opacity: 1;
                transform: scale(1) translateY(0)
            }
        }

        .modal h3 {
            font-family: var(--font-display);
            color: var(--bark);
            margin-bottom: 20px;
            font-size: 1.2rem;
            font-weight: 700;
            letter-spacing: -.2px
        }

        .modal label {
            font-size: .68rem;
            font-weight: 600;
            letter-spacing: 1.2px;
            text-transform: uppercase;
            color: var(--bark);
            display: block;
            margin-bottom: 5px;
            margin-top: 15px
        }

        .modal input {
            width: 100%;
            border: 1.5px solid #e8d5a0;
            border-radius: var(--radius-input);
            padding: 11px 14px;
            font-family: var(--font-body);
            font-size: .9rem;
            color: var(--bark);
            outline: none;
            background: var(--cream);
            transition: all var(--transition);
        }

        .modal input:focus {
            border-color: var(--gold-400);
            box-shadow: 0 0 0 4px rgba(245, 158, 11, .12), 0 4px 12px rgba(245, 158, 11, .06)
        }

        .modal-actions {
            display: flex;
            gap: 8px;
            margin-top: 22px
        }

        .btn-modal-save {
            flex: 1;
            background: linear-gradient(135deg, var(--gold-500), var(--gold-600));
            color: white;
            border: none;
            border-radius: var(--radius-input);
            padding: 11px;
            font-weight: 700;
            cursor: pointer;
            font-size: .9rem;
            transition: var(--transition);
            box-shadow: 0 4px 14px rgba(180, 83, 9, .25);
        }

        .btn-modal-save:hover {
            filter: brightness(1.12);
            transform: translateY(-2px);
            box-shadow: 0 8px 22px rgba(180, 83, 9, .35)
        }

        .btn-modal-cancel {
            background: linear-gradient(135deg, #f0e4c0, #edd9a3);
            color: var(--bark);
            border: none;
            border-radius: var(--radius-input);
            padding: 11px 18px;
            font-weight: 500;
            cursor: pointer;
            font-size: .9rem;
            transition: var(--transition);
        }

        .btn-modal-cancel:hover {
            background: linear-gradient(135deg, #e8d5a0, #e0c87a);
            transform: translateY(-1px)
        }

        /* ── ADD MONEY MODAL special style ── */
        .add-money-modal h3 {
            color: var(--bark);
        }

        .add-money-modal .btn-modal-save {
            background: linear-gradient(135deg, var(--gold-500), var(--gold-600));
            box-shadow: 0 4px 14px rgba(180, 83, 9, .25);
        }

        .add-money-modal .btn-modal-save:hover {
            box-shadow: 0 8px 22px rgba(180, 83, 9, .35);
        }

        .add-money-modal .btn-modal-save:hover {
            box-shadow: 0 8px 22px rgba(22, 163, 74, .35);
        }

        /* money-in history inside add money modal */
        .money-history {
            margin-top: 20px;
            border-top: 1px dashed #e8d5a0;
            padding-top: 14px;
        }

        .money-history-title {
            font-size: .64rem;
            font-weight: 600;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            color: var(--bark);
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .money-history-list {
            display: flex;
            flex-direction: column;
            gap: 6px;
            max-height: 180px;
            overflow-y: auto;
        }

        .money-history-list::-webkit-scrollbar {
            width: 3px
        }

        .money-history-list::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--gold-300), var(--gold-500));
            border-radius: 3px
        }

        .money-history-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 9px 12px;
            background: linear-gradient(135deg, var(--gold-50), var(--gold-100));
            border: 1px solid var(--gold-200);
            border-radius: 10px;
            gap: 10px;
            transition: all var(--transition);
        }

        .money-history-item:hover {
            background: linear-gradient(135deg, var(--gold-100), var(--gold-200));
        }

        .money-history-info {
            flex: 1;
            min-width: 0;
        }

        .money-history-note {
            font-size: .8rem;
            font-weight: 600;
            color: var(--bark);
            display: block;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .money-history-date {
            font-size: .63rem;
            color: #a08040;
            display: block;
            margin-top: 1px;
        }

        .money-history-amount {
            font-family: var(--font-display);
            font-size: .9rem;
            font-weight: 700;
            color: var(--green-600);
            white-space: nowrap;
        }

        .btn-del-money {
            background: none;
            border: none;
            cursor: pointer;
            color: #c0a060;
            font-size: 1rem;
            width: 22px;
            height: 22px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 6px;
            transition: all var(--transition);
            flex-shrink: 0;
        }

        .btn-del-money:hover {
            color: var(--danger);
            background: #fee2e2;
            transform: scale(1.2)
        }

        .no-money-history {
            font-size: .78rem;
            color: #c0a060;
            text-align: center;
            padding: 12px;
            font-style: italic;
        }

        /* ── REPORT ── */
        .report-modal {
            background: white;
            border-radius: var(--radius-card);
            padding: 30px;
            width: 100%;
            max-width: 580px;
            box-shadow: 0 30px 80px rgba(61, 32, 7, .3);
            max-height: 92vh;
            overflow-y: auto;
            animation: modalIn .38s cubic-bezier(.16, 1, .3, 1) both;
        }

        .report-modal::-webkit-scrollbar {
            width: 4px
        }

        .report-modal::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--gold-300), var(--gold-500));
            border-radius: 4px
        }

        .report-top {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 24px;
            padding-bottom: 18px;
            border-bottom: 2px solid;
            border-image: linear-gradient(90deg, var(--gold-200), var(--gold-400), transparent) 1
        }

        .report-top h3 {
            font-family: var(--font-display);
            color: var(--bark);
            font-size: 1.25rem;
            font-weight: 700;
            letter-spacing: -.2px
        }

        .btn-close-report {
            background: var(--cream);
            border: 1.5px solid #e8d5a0;
            border-radius: 30px;
            padding: 7px 16px;
            font-size: .78rem;
            color: var(--bark);
            cursor: pointer;
            font-weight: 500;
            transition: all var(--transition);
        }

        .btn-close-report:hover {
            background: #f0e4c0;
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(180, 83, 9, .1)
        }

        /* Money-in section in report */
        .report-money-in {
            background: linear-gradient(135deg, var(--gold-50), var(--gold-100));
            border: 1.5px solid var(--gold-200);
            border-radius: 16px;
            padding: 14px 18px;
            margin-bottom: 14px;
        }

        .report-money-in-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .report-money-in-title {
            font-size: .64rem;
            font-weight: 700;
            letter-spacing: 1.6px;
            text-transform: uppercase;
            color: var(--gold-600);
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .report-money-in-total {
            font-family: var(--font-display);
            font-size: 1rem;
            font-weight: 800;
            color: var(--gold-600);
        }

        .report-money-in-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 6px 0;
            border-bottom: 1px dashed rgba(22, 163, 74, .2);
            font-size: .8rem;
        }

        .report-money-in-item:last-child {
            border-bottom: none
        }

        .report-money-in-note {
            color: var(--bark);
            font-weight: 500;
        }

        .report-money-in-date {
            color: #a08040;
            font-size: .7rem;
        }

        .report-money-in-amt {
            font-family: var(--font-display);
            font-weight: 700;
            color: var(--gold-600);
        }

        .report-month {
            border: 1.5px solid #f0e4c0;
            border-radius: 18px;
            margin-bottom: 14px;
            overflow: hidden;
            transition: box-shadow var(--transition)
        }

        .report-month:hover {
            box-shadow: 0 6px 20px rgba(180, 83, 9, .08)
        }

        .report-month-hdr {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 14px 20px;
            background: linear-gradient(135deg, #fff9ec, #fffbf5, #fff9ee);
            cursor: pointer;
            user-select: none;
            transition: background var(--transition);
        }

        .report-month-hdr:hover {
            background: linear-gradient(135deg, #fff3cc, #fef9e7)
        }

        .report-month-name {
            font-weight: 600;
            font-size: .9rem;
            color: var(--bark)
        }

        .report-month-totals {
            display: flex;
            align-items: center;
            gap: 10px;
            flex-wrap: wrap
        }

        .report-total-chip {
            font-family: var(--font-display);
            font-size: .82rem;
            font-weight: 700;
            letter-spacing: -.1px;
            white-space: nowrap;
            padding: 4px 12px;
            border-radius: 20px
        }

        .chip-spent-total {
            background: linear-gradient(135deg, #fef9ec, #fef3c7);
            color: var(--green-600);
            border: 1px solid rgba(22, 163, 74, .2)
        }

        .chip-grand-total {
            background: linear-gradient(135deg, var(--bark), var(--bark-light));
            color: var(--gold-200);
            border: none
        }

        .report-month-body {
            display: none;
            padding: 14px 20px 18px
        }

        .report-month-body.open {
            display: block
        }

        .report-summary-row {
            display: flex;
            gap: 8px;
            margin-bottom: 16px;
            padding-bottom: 14px;
            border-bottom: 1px dashed #f0e4c0;
            flex-wrap: wrap
        }

        .summary-box {
            flex: 1;
            min-width: 120px;
            border-radius: 14px;
            padding: 11px 16px;
            display: flex;
            flex-direction: column;
            gap: 4px
        }

        .summary-box.spent-box {
            background: linear-gradient(135deg, #fefce8, #fef9dc);
            border: 1px solid rgba(245, 158, 11, .25)
        }

        .summary-box.loan-box {
            background: linear-gradient(135deg, #faf5ff, #f3eeff);
            border: 1px solid rgba(124, 58, 237, .2)
        }

        .summary-box.total-box {
            background: linear-gradient(135deg, #fff9ec, #fef3c7);
            border: 1px solid rgba(180, 83, 9, .18)
        }

        .summary-box.moneyin-box {
            background: linear-gradient(135deg, var(--gold-50), var(--gold-100));
            border: 1px solid var(--gold-200)
        }

        .summary-box-label {
            font-size: .57rem;
            font-weight: 600;
            letter-spacing: 1.5px;
            text-transform: uppercase
        }

        .spent-box .summary-box-label {
            color: var(--gold-600)
        }

        .loan-box .summary-box-label {
            color: var(--loan-color)
        }

        .total-box .summary-box-label {
            color: var(--bark)
        }

        .moneyin-box .summary-box-label {
            color: var(--gold-600)
        }

        .summary-box-value {
            font-family: var(--font-display);
            font-size: 1.05rem;
            font-weight: 800;
            letter-spacing: -.3px
        }

        .spent-box .summary-box-value {
            color: var(--green-600)
        }

        .loan-box .summary-box-value {
            color: var(--loan-color)
        }

        .total-box .summary-box-value {
            color: var(--bark)
        }

        .moneyin-box .summary-box-value {
            color: var(--gold-600)
        }

        .report-cat-row {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            padding: 7px 0;
            border-bottom: 1px dashed #f0e4c0;
            font-size: .85rem
        }

        .report-cat-row:last-of-type {
            border-bottom: none
        }

        .report-cat-name {
            color: var(--bark);
            font-weight: 500
        }

        .report-cat-amt {
            font-weight: 700;
            color: var(--bark);
            font-family: var(--font-display)
        }

        .report-exp-list {
            display: flex;
            flex-direction: column;
            gap: 3px;
            margin-bottom: 6px
        }

        .report-exp-item {
            display: flex;
            gap: 10px;
            font-size: .74rem;
            color: #a08040;
            padding: 4px 0 4px 14px;
            border-left: 2px solid #f0e4c0;
            align-items: center
        }

        .report-exp-item.loan-exp-row {
            border-left-color: var(--loan-border)
        }

        .rexp-type-tag {
            font-size: .55rem;
            font-weight: 700;
            letter-spacing: .5px;
            text-transform: uppercase;
            padding: 2px 6px;
            border-radius: 5px;
            flex-shrink: 0
        }

        .rtag-spent {
            background: #fef3c7;
            color: var(--gold-600)
        }

        .rtag-loan {
            background: #ede9fe;
            color: var(--loan-color)
        }

        .rexp-note {
            flex: 1;
            min-width: 0;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap
        }

        .rexp-date {
            white-space: nowrap;
            flex-shrink: 0
        }

        .rexp-amt {
            font-weight: 700;
            color: var(--bark);
            white-space: nowrap;
            flex-shrink: 0
        }

        .rexp-amt.loan-rexp {
            color: var(--loan-color)
        }

        .report-empty {
            text-align: center;
            padding: 40px;
            color: #a08040;
            font-size: .9rem
        }

        .report-grand {
            background: linear-gradient(135deg, var(--bark) 0%, #5c2b0c 50%, var(--bark-light) 100%);
            color: var(--gold-100);
            border-radius: 16px;
            padding: 16px 22px;
            margin-top: 18px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 8px 24px rgba(61, 32, 7, .25), inset 0 1px 0 rgba(253, 230, 138, .1);
        }

        .report-grand-label {
            font-size: .67rem;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            color: rgba(253, 230, 138, .55)
        }

        .report-grand-amount {
            font-family: var(--font-display);
            font-size: 1.4rem;
            font-weight: 800;
            color: var(--gold-100);
            letter-spacing: -.5px
        }

        /* ── LOGS ── */
        .logs-modal {
            background: white;
            border-radius: var(--radius-card);
            padding: 30px;
            width: 100%;
            max-width: 650px;
            box-shadow: 0 30px 80px rgba(61, 32, 7, .3);
            max-height: 92vh;
            overflow-y: auto;
            animation: modalIn .38s cubic-bezier(.16, 1, .3, 1) both;
        }

        .logs-modal::-webkit-scrollbar {
            width: 4px
        }

        .logs-modal::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--gold-300), var(--gold-500));
            border-radius: 4px
        }

        .logs-top {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
            padding-bottom: 18px;
            border-bottom: 1.5px solid #f0e4c0;
            flex-wrap: wrap;
            gap: 10px
        }

        .logs-top h3 {
            font-family: var(--font-display);
            color: var(--bark);
            font-size: 1.25rem;
            font-weight: 700;
            letter-spacing: -.2px
        }

        .logs-controls {
            display: flex;
            gap: 8px;
            align-items: center;
            flex-wrap: wrap
        }

        .logs-search {
            border: 1.5px solid #e8d5a0;
            border-radius: 30px;
            padding: 7px 16px;
            font-family: var(--font-body);
            font-size: .78rem;
            color: var(--bark);
            outline: none;
            background: var(--cream);
            width: 160px;
            transition: all var(--transition)
        }

        .logs-search:focus {
            border-color: var(--gold-400);
            box-shadow: 0 0 0 3px rgba(245, 158, 11, .1);
            width: 200px
        }

        .logs-filter-btn {
            border: 1.5px solid #e8d5a0;
            border-radius: 30px;
            padding: 7px 13px;
            font-family: var(--font-body);
            font-size: .74rem;
            font-weight: 500;
            cursor: pointer;
            color: #a08040;
            background: white;
            transition: all var(--transition);
            white-space: nowrap
        }

        .logs-filter-btn:hover {
            background: #fff9ec;
            border-color: var(--gold-400);
            color: var(--gold-600);
            transform: translateY(-1px)
        }

        .logs-filter-btn.active {
            background: linear-gradient(135deg, var(--bark), var(--bark-light));
            color: var(--gold-200);
            border-color: var(--bark);
            box-shadow: 0 4px 14px rgba(61, 32, 7, .25)
        }

        .logs-count {
            font-size: .72rem;
            color: #a08040;
            font-weight: 500;
            padding: 5px 11px;
            background: linear-gradient(135deg, #fef9ec, #fef3c7);
            border-radius: 20px;
            border: 1px solid #f0e4c0;
            white-space: nowrap
        }

        .logs-month-tabs {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
            margin-bottom: 16px
        }

        .logs-month-tab {
            border: 1.5px solid #e8d5a0;
            border-radius: 30px;
            padding: 8px 17px;
            font-family: var(--font-body);
            font-size: .78rem;
            font-weight: 600;
            cursor: pointer;
            color: #a08040;
            background: white;
            transition: all var(--transition);
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 6px
        }

        .logs-month-tab:hover {
            background: linear-gradient(135deg, #fff9ec, #fef9ee);
            border-color: var(--gold-400);
            color: var(--gold-600);
            transform: translateY(-1px)
        }

        .logs-month-tab.active {
            background: linear-gradient(135deg, var(--bark), var(--bark-light));
            color: var(--gold-200);
            border-color: var(--bark);
            box-shadow: 0 4px 14px rgba(61, 32, 7, .25)
        }

        .tab-count {
            background: rgba(255, 255, 255, .18);
            color: inherit;
            border-radius: 20px;
            padding: 1px 8px;
            font-size: .64rem;
            font-weight: 700
        }

        .logs-month-tab:not(.active) .tab-count {
            background: linear-gradient(135deg, #f0e4c0, #edd9a3);
            color: #a08040
        }

        .logs-table-wrap {
            overflow-x: auto
        }

        .logs-table {
            width: 100%;
            border-collapse: collapse;
            font-size: .82rem
        }

        .logs-table thead tr {
            background: linear-gradient(135deg, var(--bark), #5c2b0c, var(--bark-light))
        }

        .logs-table thead th {
            padding: 11px 16px;
            text-align: left;
            font-size: .59rem;
            font-weight: 600;
            letter-spacing: 1.6px;
            text-transform: uppercase;
            color: rgba(253, 230, 138, .7);
            white-space: nowrap
        }

        .logs-table thead th:first-child {
            border-radius: 12px 0 0 12px
        }

        .logs-table thead th:last-child {
            border-radius: 0 12px 12px 0;
            text-align: right
        }

        .logs-table tbody tr {
            border-bottom: 1px solid #f5ead0;
            transition: background var(--transition)
        }

        .logs-table tbody tr:last-child {
            border-bottom: none
        }

        .logs-table tbody tr:hover {
            background: linear-gradient(90deg, #fffbf0, #fff9ec)
        }

        .logs-table tbody tr.loan-row {
            background: #fdfbff
        }

        .logs-table tbody tr.loan-row:hover {
            background: linear-gradient(90deg, #f9f5ff, #f5f3ff)
        }

        .logs-table td {
            padding: 11px 16px;
            color: var(--bark);
            vertical-align: middle
        }

        .logs-table td:last-child {
            text-align: right
        }

        .log-cat-badge {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            background: linear-gradient(135deg, var(--gold-100), var(--gold-200));
            border: 1px solid var(--gold-200);
            border-radius: 20px;
            padding: 3px 11px;
            font-size: .72rem;
            font-weight: 600;
            color: var(--bark);
            white-space: nowrap;
            box-shadow: 0 1px 4px rgba(180, 83, 9, .08)
        }

        .log-note-cell {
            font-weight: 500;
            max-width: 160px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap
        }

        .log-amount-cell {
            font-family: var(--font-display);
            font-weight: 700;
            font-size: .9rem;
            letter-spacing: -.2px;
            white-space: nowrap
        }

        .log-amount-cell.is-loan {
            color: var(--loan-color)
        }

        .log-amount-cell.is-spent {
            color: var(--bark)
        }

        .log-date-cell {
            font-size: .74rem;
            color: #a08040;
            white-space: nowrap
        }

        .log-type-pill {
            font-size: .55rem;
            font-weight: 700;
            letter-spacing: .6px;
            padding: 3px 9px;
            border-radius: 20px;
            text-transform: uppercase
        }

        .pill-spent {
            background: linear-gradient(135deg, #fef3c7, #fde68a);
            color: var(--gold-600)
        }

        .pill-loan {
            background: linear-gradient(135deg, #ede9fe, #ddd6fe);
            color: var(--loan-color)
        }

        .logs-empty {
            text-align: center;
            padding: 52px 24px;
            color: #a08040
        }

        .logs-empty .orb {
            width: 68px;
            height: 68px;
            background: linear-gradient(135deg, var(--gold-100), var(--gold-200));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.9rem;
            margin: 0 auto 16px;
            box-shadow: 0 6px 20px rgba(180, 83, 9, .15)
        }

        .logs-empty p {
            font-size: .88rem;
            line-height: 1.6
        }

        .logs-grand-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: linear-gradient(135deg, var(--bark), #5c2b0c, var(--bark-light));
            color: var(--gold-100);
            border-radius: 14px;
            padding: 13px 20px;
            margin-top: 18px;
            box-shadow: 0 6px 20px rgba(61, 32, 7, .2), inset 0 1px 0 rgba(253, 230, 138, .1);
        }

        .logs-grand-label {
            font-size: .64rem;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            color: rgba(253, 230, 138, .5)
        }

        .logs-grand-val {
            font-family: var(--font-display);
            font-size: 1.25rem;
            font-weight: 800;
            color: var(--gold-100)
        }

        .empty-state {
            text-align: center;
            padding: 52px 24px;
            color: #a08040;
            display: none
        }

        .empty-state .orb {
            width: 88px;
            height: 88px;
            background: linear-gradient(135deg, var(--gold-100), var(--gold-200), var(--gold-300));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.4rem;
            margin: 0 auto 18px;
            box-shadow: 0 12px 32px rgba(180, 83, 9, .2), 0 0 0 8px rgba(253, 230, 138, .1);
            animation: orbPulse 3s ease-in-out infinite;
        }

        @keyframes orbPulse {

            0%,
            100% {
                transform: scale(1)
            }

            50% {
                transform: scale(1.06)
            }
        }

        .empty-state p {
            font-size: .9rem;
            line-height: 1.7
        }

        .empty-state p strong {
            color: var(--bark);
            font-weight: 600
        }

        .backup-modal {
            background: white;
            border-radius: var(--radius-card);
            padding: 26px 22px;
            width: 100%;
            max-width: 440px;
            box-shadow: 0 30px 80px rgba(61, 32, 7, .3);
            animation: modalIn .38s cubic-bezier(.16, 1, .3, 1) both;
            max-height: 92vh;
            overflow-y: auto
        }

        .backup-modal::-webkit-scrollbar {
            width: 3px
        }

        .backup-modal::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--gold-300), var(--gold-500));
            border-radius: 3px
        }

        .backup-modal h3 {
            font-family: var(--font-display);
            color: var(--bark);
            font-size: 1.2rem;
            font-weight: 700;
            letter-spacing: -.2px;
            margin-bottom: 6px
        }

        .backup-subtitle {
            font-size: .78rem;
            color: #a08040;
            margin-bottom: 24px;
            line-height: 1.5
        }

        .backup-section {
            border: 1.5px solid #f0e4c0;
            border-radius: 18px;
            padding: 20px;
            margin-bottom: 16px;
            background: linear-gradient(135deg, #fffdf5, white)
        }

        .backup-section-title {
            font-size: .64rem;
            font-weight: 700;
            letter-spacing: 1.8px;
            text-transform: uppercase;
            color: var(--bark);
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 7px
        }

        .backup-section-desc {
            font-size: .76rem;
            color: #a08040;
            margin-bottom: 14px;
            line-height: 1.5
        }

        .backup-meta {
            font-size: .7rem;
            color: #c0a060;
            margin-top: 9px;
            font-style: italic
        }

        .btn-export {
            width: 100%;
            background: linear-gradient(135deg, var(--green-600), #15803d);
            color: #dcfce7;
            border: none;
            border-radius: var(--radius-input);
            padding: 12px;
            font-family: var(--font-body);
            font-weight: 600;
            font-size: .88rem;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 7px;
            box-shadow: 0 4px 14px rgba(22, 163, 74, .25)
        }

        .btn-export:hover {
            filter: brightness(1.12);
            transform: translateY(-2px);
            box-shadow: 0 8px 22px rgba(22, 163, 74, .35)
        }

        .import-drop-zone {
            border: 2px dashed #e8d5a0;
            border-radius: 14px;
            padding: 24px 18px;
            text-align: center;
            font-size: .78rem;
            color: #a08040;
            cursor: pointer;
            transition: all var(--transition);
            margin-bottom: 10px;
            background: var(--cream);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 7px;
            width: 100%;
            box-sizing: border-box
        }

        .import-drop-zone:hover,
        .import-drop-zone.dragover {
            border-color: var(--gold-400);
            background: linear-gradient(135deg, #fff9ec, #fef9ee);
            color: var(--gold-600)
        }

        .import-drop-zone .drop-icon {
            font-size: 2.2rem;
            line-height: 1
        }

        .import-drop-zone input[type=file] {
            display: none
        }

        .import-drop-zone .drop-hint {
            font-size: .67rem;
            color: #c0a060
        }

        .import-warning {
            background: linear-gradient(135deg, #fff7ed, #fff4e8);
            border: 1px solid #fed7aa;
            border-radius: 12px;
            padding: 11px 15px;
            font-size: .74rem;
            color: #9a3412;
            line-height: 1.5;
            margin-bottom: 10px;
            display: none
        }

        .import-warning.show {
            display: block
        }

        .btn-confirm-import {
            width: 100%;
            background: linear-gradient(135deg, var(--bark), var(--bark-light));
            color: var(--gold-200);
            border: none;
            border-radius: var(--radius-input);
            padding: 12px;
            font-family: var(--font-body);
            font-weight: 600;
            font-size: .88rem;
            cursor: pointer;
            transition: var(--transition);
            display: none;
            align-items: center;
            justify-content: center;
            gap: 7px;
            box-shadow: 0 4px 14px rgba(61, 32, 7, .2)
        }

        .btn-confirm-import.show {
            display: flex
        }

        .btn-confirm-import:hover {
            filter: brightness(1.15);
            transform: translateY(-2px)
        }

        .backup-close-row {
            margin-top: 18px;
            text-align: center
        }

        .toast {
            position: fixed;
            bottom: 28px;
            left: 50%;
            transform: translateX(-50%) translateY(120px);
            background: linear-gradient(135deg, var(--bark), #5c2b0c, var(--bark-light));
            color: var(--gold-100);
            padding: 11px 28px;
            border-radius: 30px;
            font-size: .84rem;
            font-weight: 600;
            z-index: 500;
            transition: transform .38s cubic-bezier(.16, 1, .3, 1);
            white-space: nowrap;
            pointer-events: none;
            box-shadow: 0 12px 32px rgba(61, 32, 7, .4), inset 0 1px 0 rgba(253, 230, 138, .15);
            border: 1px solid rgba(253, 230, 138, .15);
        }

        .toast.show {
            transform: translateX(-50%) translateY(0)
        }

        #editExpTypeSpent.active-spent,
        #editExpTypeLoan.active-loan {
            box-shadow: 0 3px 10px rgba(0, 0, 0, .12);
        }

        @media(max-width:480px) {
            .header-btns {
                gap: 5px
            }

            .btn-report,
            .btn-logs {
                padding: 7px 11px;
                font-size: .72rem
            }

            .stats-grid {
                grid-template-columns: 1fr 1fr
            }

            .budget-card {
                padding: 20px
            }

            .log-type-row {
                gap: 5px
            }

            .logs-search {
                width: 120px
            }

            .log-note-cell {
                max-width: 100px
            }

            .backup-modal {
                padding: 20px 16px;
                border-radius: 18px
            }
        }

        .btn-add-budget-cat {
            background: linear-gradient(135deg, rgba(245, 158, 11, .18), rgba(180, 83, 9, .25));
            border: 1.5px solid rgba(253, 230, 138, .5);
            color: var(--bark);
            border-radius: 9px;
            padding: 7px 14px;
            font-size: .73rem;
            cursor: pointer;
            font-weight: 600;
            transition: all var(--transition);
        }

        .btn-add-budget-cat:hover {
            background: linear-gradient(135deg, rgba(245, 158, 11, .35), rgba(180, 83, 9, .4));
            border-color: var(--gold-400);
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(180, 83, 9, .15)
        }

        .btn-deduct-budget-cat {
            background: linear-gradient(135deg, rgba(220, 38, 38, .15), rgba(153, 27, 27, .22));
            border: 1.5px solid rgba(252, 165, 165, .5);
            color: var(--bark);
            border-radius: 9px;
            padding: 7px 14px;
            font-size: .73rem;
            cursor: pointer;
            font-weight: 600;
            transition: all var(--transition);
        }

        .btn-deduct-budget-cat:hover {
            background: linear-gradient(135deg, rgba(220, 38, 38, .32), rgba(153, 27, 27, .4));
            border-color: rgba(239, 68, 68, .8);
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(153, 27, 27, .15)
        }
    </style>
</head>

<body>

    <div class="particle"
        style="width:300px;height:300px;left:5%;top:20%;animation-duration:18s;animation-delay:0s;opacity:.4"></div>
    <div class="particle"
        style="width:200px;height:200px;left:75%;top:10%;animation-duration:22s;animation-delay:-7s;opacity:.3"></div>
    <div class="particle"
        style="width:150px;height:150px;left:40%;top:60%;animation-duration:15s;animation-delay:-3s;opacity:.25"></div>
    <div class="particle"
        style="width:100px;height:100px;left:90%;top:50%;animation-duration:20s;animation-delay:-11s;opacity:.2"></div>

    <header class="header">
        <div class="header-inner">
            <div class="header-brand">
                <div class="header-sunflower">🌻</div>
                <div>
                    <div class="header-title">Maribank</div>
                    <span class="header-subtitle">Bank Budget Tracker</span>
                </div>
            </div>
        </div>
    </header>

    <div class="container">

        <div class="budget-card">
            <div class="budget-card-deco"></div>
            <div class="budget-card-sun">🌻</div>
            <div class="budget-eyebrow">Total Budget · Maribank
                <button id="eyeBtn" onclick="toggleVisibility()"
                    style="background:none;border:none;cursor:pointer;margin-left:6px;opacity:0.6;transition:opacity 0.2s;display:flex;align-items:center;"
                    title="Toggle visibility">
                    <svg id="eyeIcon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
                        fill="none" stroke="rgba(253,230,138,0.7)" stroke-width="2" stroke-linecap="round"
                        stroke-linejoin="round">
                        <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
                        <circle cx="12" cy="12" r="3" />
                    </svg>
                </button>
            </div>

            <div class="budget-amount-row">
                <div class="budget-amount" id="budgetDisplay">₱0.00</div>
                <button class="btn-add-money" onclick="openAddMoney()">＋ Add Money</button>
            </div>

            <div class="stats-grid">
                <div class="stat-pill">
                    <span class="stat-label">Total Spent</span>
                    <span class="stat-value red" id="totalSpent">₱0.00</span>
                </div>
                <div class="stat-pill">
                    <span class="stat-label">Remaining (Maribank)</span>
                    <span class="stat-value green" id="totalRemaining">₱0.00</span>
                </div>
                <div class="stat-pill">
                    <span class="stat-label">Remaining (Categories)</span>
                    <span class="stat-value yellow" id="catTotalRemaining">₱0.00</span>
                    <span class="stat-hint">sum of all category budgets left</span>
                </div>
                <div class="stat-pill" id="diffPill">
                    <span class="stat-label" id="diffLabel">Excess</span>
                    <span class="stat-value green" id="diffVal">₱0.00</span>
                    <span class="stat-hint" id="diffHint">unallocated from Maribank</span>
                </div>
            </div>
            <div class="progress-section">
                <div class="progress-track">
                    <div class="progress-bar" id="totalProgress" style="width:0%"></div>
                </div>
                <div class="progress-meta">
                    <span class="progress-label">Budget utilisation</span>
                    <span class="progress-label" id="progressPct">0% used</span>
                </div>
            </div>
        </div>

        <div class="action-row">
            <div class="dropdown-wrap" id="dataDropdown">
                <button class="btn-data-menu" onclick="toggleDataMenu()">🗂️ Data ▾</button>
                <div class="data-menu" id="dataMenu">
                    <button class="data-menu-item" onclick="openBackup();closeDataMenu()">💾 Backup & Restore</button>
                    <button class="data-menu-item danger-item" onclick="resetAll();closeDataMenu()">🗑️ Reset
                        Everything</button>
                </div>
            </div>
            <button class="btn-logs" onclick="openLogs()">🗒️ Logs</button>
            <button class="btn-report" onclick="openReport()">📊 Reports</button>
        </div>

        <div class="section-header">
            <div class="section-title">Budget Categories</div>
            <button class="btn-add-cat" onclick="toggleAddForm()">＋ Add Category</button>
        </div>

        <div class="add-cat-form" id="addCatForm">
            <div class="form-row">
                <input type="text" id="newCatName" placeholder="Category name (e.g. Groceries)" maxlength="30">
                <input type="number" id="newCatBudget" placeholder="Budget (₱)" min="0" step="0.01">
            </div>
            <div class="form-row" style="margin-top:10px;">
                <button class="btn-form-add" onclick="addCategory()">Add Category</button>
                <button class="btn-form-cancel" onclick="toggleAddForm()">Cancel</button>
            </div>
        </div>

        <div class="categories" id="categoriesList"></div>
        <div class="empty-state" id="emptyState">
            <div class="orb">🌱</div>
            <p><strong>No categories yet.</strong><br>Add your first budget category above<br>to start tracking your
                spending.</p>
        </div>

    </div>

    <!-- Add Money Modal -->
    <div class="modal-overlay" id="addMoneyModal">
        <div class="modal add-money-modal">
            <h3>🌻 Add Money</h3>
            <label>Note / Source</label>
            <input type="text" id="addMoneyNote" placeholder="e.g. Salary, Allowance, Transfer" maxlength="60">
            <label>Amount to Add (₱)</label>
            <input type="number" id="addMoneyAmount" placeholder="0.00" min="0" step="0.01">
            <label>Date</label>
            <input type="date" id="addMoneyDate">
            <div class="modal-actions">
                <button class="btn-modal-cancel" onclick="closeAddMoney()">Cancel</button>
                <button class="btn-modal-save" onclick="saveAddMoney()">Add to Budget</button>
            </div>
            <!-- Money-in history -->
            <div class="money-history">
                <div class="money-history-title">
                    <span>📋 Money-In History</span>
                </div>
                <div class="money-history-list" id="moneyHistoryList">
                    <div class="no-money-history">No money added yet</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Edit Modal -->
    <div class="modal-overlay" id="editModal">
        <div class="modal">
            <h3>Edit Category</h3>
            <label>Category Name</label>
            <input type="text" id="editName" maxlength="30">
            <label>Category Budget (₱)</label>
            <input type="number" id="editBudgetAmount" min="0" step="0.01">
            <div class="modal-actions">
                <button class="btn-modal-cancel" onclick="closeEditModal()">Cancel</button>
                <button class="btn-modal-save" onclick="saveEdit()">Save Changes</button>
            </div>
        </div>
    </div>

    <!-- Report Modal -->
    <div class="modal-overlay" id="reportModal">
        <div class="report-modal">
            <div class="report-top">
                <h3>📊 Monthly Report</h3>
                <button class="btn-close-report" onclick="closeReport()">✕ Close</button>
            </div>
            <div id="reportBody"></div>
        </div>
    </div>

    <!-- Logs Modal -->
    <div class="modal-overlay" id="logsModal">
        <div class="logs-modal">
            <div class="logs-top">
                <h3>🗒️ All Logs</h3>
                <div class="logs-controls">
                    <input type="text" class="logs-search" id="logsSearch" placeholder="🔍 Search…"
                        oninput="renderLogsTable()">
                    <button class="logs-filter-btn active" id="filterAll" onclick="setLogsFilter('all')">All</button>
                    <button class="logs-filter-btn" id="filterSpent" onclick="setLogsFilter('spent')">💸 Spent</button>
                    <button class="logs-filter-btn" id="filterLoan" onclick="setLogsFilter('loan')">💜 Loan</button>
                    <span class="logs-count" id="logsCount">0 entries</span>
                    <button class="btn-close-report" onclick="closeLogs()">✕ Close</button>
                </div>
            </div>
            <div class="logs-month-tabs" id="logsMonthTabs"></div>
            <div class="logs-table-wrap" id="logsTableWrap"></div>
        </div>
    </div>

    <!-- Backup Modal -->
    <div class="modal-overlay" id="backupModal">
        <div class="backup-modal">
            <h3>💾 Backup & Restore</h3>
            <p class="backup-subtitle">Export your data to a file to keep it safe, or import a previously saved backup
                to restore your records.</p>
            <div class="backup-section">
                <div class="backup-section-title">📤 Export Data</div>
                <p class="backup-section-desc">Download all your budget categories, expenses, and settings as a
                    <strong>.json</strong> file.
                </p>
                <button class="btn-export" onclick="exportData()">⬇️ Download Backup File</button>
                <div class="backup-meta" id="exportMeta"></div>
            </div>
            <div class="backup-section">
                <div class="backup-section-title">📥 Import / Restore</div>
                <p class="backup-section-desc">Select a previously exported <strong>.json</strong> backup file to
                    restore your data.</p>
                <div class="import-drop-zone" id="importDropZone" ondragover="handleDragOver(event)"
                    ondragleave="handleDragLeave(event)" ondrop="handleDrop(event)"
                    onclick="document.getElementById('importFileInput').click()">
                    <span class="drop-icon">📂</span>
                    <strong>Tap to choose file</strong>
                    <span class="drop-hint">Only Maribank .json backup files</span>
                    <input type="file" id="importFileInput" accept=".json" onchange="handleFileSelect(event)"
                        style="display:none;">
                </div>
                <div class="import-warning" id="importWarning"></div>
                <button class="btn-confirm-import" id="btnConfirmImport" onclick="confirmImport()">✅ Restore (Replace
                    all)</button>
                <button class="btn-confirm-import" id="btnMergeImport" onclick="mergeImport()"
                    style="background:linear-gradient(135deg,#7c3aed,#6d28d9);margin-top:6px;">➕ Merge into
                    existing</button>
            </div>
            <div class="backup-close-row">
                <button class="btn-modal-cancel" onclick="closeBackup()">Close</button>
            </div>
        </div>
    </div>

    <!-- Add Category Budget Modal -->
    <div class="modal-overlay" id="addCatBudgetModal">
        <div class="modal" style="max-width:340px;">
            <h3>＋ Add to Budget</h3>
            <label>Amount to Add (₱)</label>
            <input type="number" id="addCatBudgetAmount" placeholder="0.00" min="0" step="0.01">
            <div class="modal-actions">
                <button class="btn-modal-cancel" onclick="closeAddCatBudget()">Cancel</button>
                <button class="btn-modal-save" onclick="saveAddCatBudget()">Add to Budget</button>
            </div>
        </div>
    </div>

    <!-- Deduct Category Budget Modal -->
    <div class="modal-overlay" id="deductCatBudgetModal">
        <div class="modal" style="max-width:340px;">
            <h3>－ Deduct from Budget</h3>
            <label>Amount to Deduct (₱)</label>
            <input type="number" id="deductCatBudgetAmount" placeholder="0.00" min="0" step="0.01">
            <p id="deductCatBudgetHint" style="font-size:.72rem;color:var(--bark);opacity:.7;margin:8px 0 0;"></p>
            <div class="modal-actions">
                <button class="btn-modal-cancel" onclick="closeDeductCatBudget()">Cancel</button>
                <button class="btn-modal-save" onclick="saveDeductCatBudget()">Deduct from Budget</button>
            </div>
        </div>
    </div>

    <!-- Edit Expense Modal -->
    <div class="modal-overlay" id="editExpModal">
        <div class="modal">
            <h3>Edit Expense</h3>
            <label>Note</label>
            <input type="text" id="editExpNote" maxlength="60">
            <label>Amount (₱)</label>
            <input type="number" id="editExpAmount" min="0" step="0.01">
            <label>Type</label>
            <div style="display:flex;gap:8px;margin-bottom:2px;">
                <button type="button" id="editExpTypeSpent" class="log-type-btn active-spent"
                    onclick="setEditExpType('spent')">💸 Spent</button>
                <button type="button" id="editExpTypeLoan" class="log-type-btn" onclick="setEditExpType('loan')">💜
                    Loan</button>
            </div>
            <label>Date</label>
            <input type="date" id="editExpDate">
            <div class="modal-actions">
                <button class="btn-modal-cancel" onclick="closeEditExpModal()">Cancel</button>
                <button class="btn-modal-save" onclick="saveEditExp()">Save Changes</button>
            </div>
        </div>
    </div>

    <div class="toast" id="toast"></div>

    <script>
        // state now includes moneyIn array: [{id, note, amount, dateRaw, date, loggedAt}]
        let state = { totalBudget: 0, categories: [], moneyIn: [] };
        let editingId = null;

        function save() { localStorage.setItem('maribank_v3', JSON.stringify(state)) }
        function load() {
            const raw = localStorage.getItem('maribank_v3') || localStorage.getItem('maribank_tracker_v2') || localStorage.getItem('maribank_tracker');
            if (raw) { try { const parsed = JSON.parse(raw); state = { moneyIn: [], ...parsed }; } catch (e) { } }
        }
        function uid() { return Date.now().toString(36) + Math.random().toString(36).slice(2, 6) }
        function fmt(n) { return '₱' + Number(n).toLocaleString('en-PH', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) }
        function todayISO() { const d = new Date(); return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}` }
        const MONTHS_SHORT = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
        const MONTHS_FULL = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
        function isoToLabel(iso) { const [y, m, d] = iso.split('-'); return `${MONTHS_SHORT[parseInt(m) - 1]} ${parseInt(d)}, ${y}` }
        function isoToMonthKey(iso) { return iso ? iso.slice(0, 7) : '0000-00' }
        function fmtLogDate(e) {
            if (e.dateRaw) { const [y, m, d] = e.dateRaw.split('-'); return `${m}/${d}/${y.slice(2)}` }
            if (e.date) { const parts = e.date.replace(',', '').split(' '); const mi = MONTHS_SHORT.indexOf(parts[0]); if (mi >= 0) { const m = String(mi + 1).padStart(2, '0'); const d = String(parts[1]).padStart(2, '0'); const y = parts[2] ? parts[2].slice(2) : '??'; return `${m}/${d}/${y}` } }
            return '—'
        }
        function fmtLogTime(e) {
            if (e.loggedAt) { const d = new Date(e.loggedAt); let h = d.getHours(), min = d.getMinutes(); const ampm = h >= 12 ? 'PM' : 'AM'; h = h % 12 || 12; return `${String(h).padStart(2, '0')}:${String(min).padStart(2, '0')} ${ampm}` }
            return null
        }
        function expType(e) { return e.type || 'spent' }
        function catSpentOnly(cat) { return cat.expenses.filter(e => expType(e) === 'spent').reduce((s, e) => s + Number(e.amount), 0) }
        function catLoanOnly(cat) { return cat.expenses.filter(e => expType(e) === 'loan').reduce((s, e) => s + Number(e.amount), 0) }
        function catSpent(cat) { return cat.expenses.reduce((s, e) => s + Number(e.amount), 0) }
        function totalSpentAll() { return state.categories.reduce((s, c) => s + catSpent(c), 0) }
        function catTotalRem() { return state.categories.reduce((s, c) => s + Math.max(0, c.budget - catSpent(c)), 0) }
        function isDepleted(cat) { return cat.budget > 0 && catSpent(cat) >= cat.budget }
        function parseFallbackKey(dateStr) {
            if (!dateStr) return '0000-00'; const parts = dateStr.replace(',', '').split(' ');
            if (parts.length < 3) return '0000-00';
            return `${parts[2]}-${String(MONTHS_SHORT.indexOf(parts[0]) + 1).padStart(2, '0')}`;
        }

        function showToast(msg) {
            const t = document.getElementById('toast');
            t.textContent = msg; t.classList.add('show');
            setTimeout(() => t.classList.remove('show'), 2400);
        }

        // ── ADD MONEY ──
        function openAddMoney() {
            document.getElementById('addMoneyNote').value = '';
            document.getElementById('addMoneyAmount').value = '';
            document.getElementById('addMoneyDate').value = todayISO();
            renderMoneyHistory();
            document.getElementById('addMoneyModal').classList.add('open');
            setTimeout(() => document.getElementById('addMoneyNote').focus(), 100);
        }

        function closeAddMoney() {
            document.getElementById('addMoneyModal').classList.remove('open');
        }

        function saveAddMoney() {
            const note = document.getElementById('addMoneyNote').value.trim();
            const amount = parseFloat(document.getElementById('addMoneyAmount').value);
            const dateISO = document.getElementById('addMoneyDate').value;
            if (!note) { showToast('Add a note/source'); return }
            if (isNaN(amount) || amount <= 0) { showToast('Enter a valid amount'); return }
            if (!dateISO) { showToast('Pick a date'); return }
            if (!state.moneyIn) state.moneyIn = [];
            const entry = { id: uid(), note, amount, dateRaw: dateISO, date: isoToLabel(dateISO), loggedAt: Date.now() };
            state.moneyIn.unshift(entry);
            state.totalBudget = (Number(state.totalBudget) || 0) + amount;
            save();
            document.getElementById('addMoneyAmount').value = '';
            document.getElementById('addMoneyNote').value = '';
            renderMoneyHistory();
            renderBudgetCard();
            showToast(`+${fmt(amount)} added to budget 💚`);
        }

        function deleteMoneyIn(id) {
            const entry = (state.moneyIn || []).find(e => e.id === id);
            if (!entry) return;
            if (!confirm(`Remove "${entry.note}" (${fmt(entry.amount)}) from budget history?`)) return;
            state.totalBudget = Math.max(0, (Number(state.totalBudget) || 0) - Number(entry.amount));
            state.moneyIn = (state.moneyIn || []).filter(e => e.id !== id);
            save();
            renderMoneyHistory();
            renderBudgetCard();
            showToast('Entry removed');
        }

        function renderMoneyHistory() {
            const list = document.getElementById('moneyHistoryList');
            const entries = state.moneyIn || [];
            if (entries.length === 0) {
                list.innerHTML = '<div class="no-money-history">No money added yet</div>';
                return;
            }
            list.innerHTML = entries.map(e => `
                <div class="money-history-item">
                    <div class="money-history-info">
                        <span class="money-history-note">${e.note}</span>
                        <span class="money-history-date">📅 ${e.date}</span>
                    </div>
                    <span class="money-history-amount">${fmt(e.amount)}</span>
                    <button class="btn-del-money" onclick="deleteMoneyIn('${e.id}')" title="Remove">×</button>
                </div>
            `).join('');
        }

        document.getElementById('addMoneyAmount').addEventListener('keydown', e => {
            if (e.key === 'Enter') saveAddMoney();
        });

        function toggleAddForm() {
            const f = document.getElementById('addCatForm'); f.classList.toggle('open');
            if (f.classList.contains('open')) document.getElementById('newCatName').focus();
        }

        function addCategory() {
            const name = document.getElementById('newCatName').value.trim();
            const budget = parseFloat(document.getElementById('newCatBudget').value);
            if (!name) { showToast('Enter a category name'); return }
            if (isNaN(budget) || budget < 0) { showToast('Enter a valid budget'); return }
            state.categories.push({ id: uid(), name, budget, expenses: [] }); save();
            document.getElementById('newCatName').value = ''; document.getElementById('newCatBudget').value = '';
            document.getElementById('addCatForm').classList.remove('open');
            render(); showToast(`${name} added! 🌻`);
        }

        function setLogType(catId, type, el) {
            document.querySelectorAll(`[data-type-group="${catId}"]`).forEach(b => { b.removeAttribute('data-active-type'); b.className = 'log-type-btn' });
            el.setAttribute('data-active-type', catId); el.dataset.type = type;
            el.className = 'log-type-btn ' + (type === 'spent' ? 'active-spent' : 'active-loan');
        }

        function logExpense(catId) {
            const noteEl = document.getElementById('expNote_' + catId);
            const amtEl = document.getElementById('expAmt_' + catId);
            const dateEl = document.getElementById('expDate_' + catId);
            const typeBtn = document.querySelector(`[data-active-type="${catId}"]`);
            const note = noteEl.value.trim(); const amount = parseFloat(amtEl.value);
            const dateISO = dateEl.value; const type = typeBtn ? typeBtn.dataset.type : 'spent';
            if (!note) { showToast('Add a note'); return }
            if (isNaN(amount) || amount <= 0) { showToast('Enter a valid amount'); return }
            if (!dateISO) { showToast('Pick a date'); return }
            const cat = state.categories.find(c => c.id === catId); if (!cat) return;
            cat.expenses.unshift({ id: uid(), note, amount, type, date: isoToLabel(dateISO), dateRaw: dateISO, loggedAt: Date.now() });
            save(); noteEl.value = ''; amtEl.value = '';
            renderCatBody(catId); renderBudgetCard();
            showToast(type === 'loan' ? 'Loan recorded 💜' : 'Expense logged ✓');
        }

        function deleteExpense(catId, expId) {
            const cat = state.categories.find(c => c.id === catId); if (!cat) return;
            cat.expenses = cat.expenses.filter(e => e.id !== expId);
            save(); renderCatBody(catId); renderBudgetCard(); showToast('Removed');
        }

        function openEdit(catId) {
            const cat = state.categories.find(c => c.id === catId); if (!cat) return;
            editingId = catId; document.getElementById('editName').value = cat.name;
            document.getElementById('editBudgetAmount').value = cat.budget;
            document.getElementById('editModal').classList.add('open');
        }
        function closeEditModal() { document.getElementById('editModal').classList.remove('open'); editingId = null }
        function saveEdit() {
            const cat = state.categories.find(c => c.id === editingId); if (!cat) return;
            const name = document.getElementById('editName').value.trim();
            const budget = parseFloat(document.getElementById('editBudgetAmount').value);
            if (!name || isNaN(budget) || budget < 0) { showToast('Fill in all fields'); return }
            cat.name = name; cat.budget = budget; save(); closeEditModal(); render(); showToast('Category updated ✓');
        }

        function deleteCategory(catId) {
            if (!confirm('Delete this category and all its expenses?')) return;
            state.categories = state.categories.filter(c => c.id !== catId); save(); render(); showToast('Category deleted');
        }

        function toggleCat(catId) {
            const body = document.getElementById('catBody_' + catId); const chev = document.getElementById('chev_' + catId);
            if (body) { body.classList.toggle('open'); if (chev) chev.classList.toggle('up') }
        }

        let logsFilter = 'all'; let logsActiveMonth = null;
        const CAT_ICONS = ['💰'];

        function openLogs() {
            document.getElementById('logsModal').classList.add('open');
            document.getElementById('logsSearch').value = ''; logsFilter = 'all';
            ['filterAll', 'filterSpent', 'filterLoan'].forEach(id => document.getElementById(id).classList.remove('active'));
            document.getElementById('filterAll').classList.add('active');
            logsActiveMonth = todayISO().slice(0, 7);
            renderLogsMonthTabs(); renderLogsTable();
        }
        function closeLogs() { document.getElementById('logsModal').classList.remove('open') }

        function setLogsFilter(f) {
            logsFilter = f;
            ['filterAll', 'filterSpent', 'filterLoan'].forEach(id => document.getElementById(id).classList.remove('active'));
            document.getElementById(f === 'all' ? 'filterAll' : f === 'spent' ? 'filterSpent' : 'filterLoan').classList.add('active');
            renderLogsTable();
        }
        function setLogsMonth(key) { logsActiveMonth = key; renderLogsMonthTabs(); renderLogsTable() }

        function renderLogsMonthTabs() {
            const monthSet = new Set();
            state.categories.forEach(cat => { cat.expenses.forEach(e => { const key = e.dateRaw ? isoToMonthKey(e.dateRaw) : parseFallbackKey(e.date); if (key && key !== '0000-00') monthSet.add(key) }) });
            const currentKey = todayISO().slice(0, 7); monthSet.add(currentKey);
            const sortedKeys = Array.from(monthSet).sort((a, b) => b.localeCompare(a));
            if (!monthSet.has(logsActiveMonth)) logsActiveMonth = sortedKeys[0];
            const tabsEl = document.getElementById('logsMonthTabs');
            tabsEl.innerHTML = sortedKeys.map(key => {
                const [y, m] = key.split('-'); const label = `${MONTHS_FULL[parseInt(m) - 1]} ${y}`;
                let cnt = 0; state.categories.forEach(cat => { cat.expenses.forEach(e => { const k = e.dateRaw ? isoToMonthKey(e.dateRaw) : parseFallbackKey(e.date); if (k === key) cnt++ }) });
                const isActive = key === logsActiveMonth;
                return `<button class="logs-month-tab${isActive ? ' active' : ''}" onclick="setLogsMonth('${key}')">📅 ${label}<span class="tab-count">${cnt}</span></button>`;
            }).join('');
        }

        function renderLogsTable() {
            const wrap = document.getElementById('logsTableWrap');
            const search = (document.getElementById('logsSearch').value || '').toLowerCase();
            let allLogs = [];
            state.categories.forEach((cat, ci) => {
                cat.expenses.forEach(e => {
                    const key = e.dateRaw ? isoToMonthKey(e.dateRaw) : parseFallbackKey(e.date);
                    if (key === logsActiveMonth) allLogs.push({ ...e, type: expType(e), catName: cat.name, catIcon: CAT_ICONS[ci % CAT_ICONS.length] });
                });
            });
            allLogs.sort((a, b) => { if (a.loggedAt && b.loggedAt) return b.loggedAt - a.loggedAt; if (a.loggedAt) return -1; if (b.loggedAt) return 1; return (b.dateRaw || '').localeCompare(a.dateRaw || '') });
            if (logsFilter !== 'all') allLogs = allLogs.filter(e => e.type === logsFilter);
            if (search) allLogs = allLogs.filter(e => e.note.toLowerCase().includes(search) || e.catName.toLowerCase().includes(search) || fmt(e.amount).includes(search));
            document.getElementById('logsCount').textContent = `${allLogs.length} entr${allLogs.length === 1 ? 'y' : 'ies'}`;
            if (allLogs.length === 0) { wrap.innerHTML = `<div class="logs-empty"><div class="orb">🌱</div><p>No logs for this month yet.</p></div>`; return }
            const grandTotal = allLogs.reduce((s, e) => s + Number(e.amount), 0);
            const rows = allLogs.map(e => {
                const isLoan = e.type === 'loan'; const dateStr = fmtLogDate(e); const timeStr = fmtLogTime(e);
                const dateTimeStr = timeStr ? `${dateStr}<br><span style="font-size:.65rem;color:#c0a0a0;">${timeStr}</span>` : dateStr;
                return `<tr class="${isLoan ? 'loan-row' : ''}">
                    <td><span class="log-cat-badge">${e.catIcon} ${e.catName}</span></td>
                    <td class="log-note-cell" title="${e.note}">${e.note}</td>
                    <td><span class="log-type-pill ${isLoan ? 'pill-loan' : 'pill-spent'}">${isLoan ? 'Loan' : 'Spent'}</span></td>
                    <td class="log-amount-cell ${isLoan ? 'is-loan' : 'is-spent'}">${fmt(e.amount)}</td>
                    <td class="log-date-cell">${dateTimeStr}</td>
                </tr>`;
            }).join('');
            wrap.innerHTML = `<table class="logs-table"><thead><tr><th>Category</th><th>Note</th><th>Type</th><th>Amount</th><th>Date / Time</th></tr></thead><tbody>${rows}</tbody></table>
            <div class="logs-grand-row"><span class="logs-grand-label">Total for this month</span><span class="logs-grand-val">${fmt(grandTotal)}</span></div>`;
        }

        function openReport() { buildReport(); document.getElementById('reportModal').classList.add('open') }
        function closeReport() { document.getElementById('reportModal').classList.remove('open') }
        function toggleReportMonth(id) { const el = document.getElementById(id); if (el) el.classList.toggle('open') }

        function buildReport() {
            const body = document.getElementById('reportBody');
            const allExp = [];
            state.categories.forEach(cat => cat.expenses.forEach(e => allExp.push({ ...e, type: expType(e), catName: cat.name })));

            // collect all months from both expenses and money-in
            const monthSet = new Set();
            allExp.forEach(e => { const key = e.dateRaw ? isoToMonthKey(e.dateRaw) : parseFallbackKey(e.date); if (key && key !== '0000-00') monthSet.add(key) });
            (state.moneyIn || []).forEach(e => { if (e.dateRaw) monthSet.add(isoToMonthKey(e.dateRaw)) });

            if (monthSet.size === 0 && allExp.length === 0) { body.innerHTML = '<div class="report-empty">🌱 No expenses or money added yet.</div>'; return }

            const sortedKeys = Array.from(monthSet).sort((a, b) => b.localeCompare(a));
            const grandTotal = allExp.reduce((s, e) => s + Number(e.amount), 0);
            const grandSpent = allExp.filter(e => e.type === 'spent').reduce((s, e) => s + Number(e.amount), 0);
            const grandLoan = allExp.filter(e => e.type === 'loan').reduce((s, e) => s + Number(e.amount), 0);
            const grandMoneyIn = (state.moneyIn || []).reduce((s, e) => s + Number(e.amount), 0);

            let html = '';
            sortedKeys.forEach((key, idx) => {
                const [y, m] = key.split('-'); const monthLabel = `${MONTHS_FULL[parseInt(m) - 1]} ${y}`;

                // money-in for this month
                const monthMoneyIn = (state.moneyIn || []).filter(e => e.dateRaw && isoToMonthKey(e.dateRaw) === key);
                const monthMoneyInTotal = monthMoneyIn.reduce((s, e) => s + Number(e.amount), 0);

                // expenses for this month
                const exps = allExp.filter(e => {
                    const k = e.dateRaw ? isoToMonthKey(e.dateRaw) : parseFallbackKey(e.date);
                    return k === key;
                });
                const monthTotal = exps.reduce((s, e) => s + Number(e.amount), 0);
                const monthSpent = exps.filter(e => e.type === 'spent').reduce((s, e) => s + Number(e.amount), 0);
                const monthLoan = exps.filter(e => e.type === 'loan').reduce((s, e) => s + Number(e.amount), 0);

                // money-in section for this month
                let moneyInHtml = '';
                if (monthMoneyIn.length > 0) {
                    const items = monthMoneyIn.map(e =>
                        `<div class="report-money-in-item">
                            <span class="report-money-in-note">${e.note}</span>
                            <span class="report-money-in-date">${e.date}</span>
                            <span class="report-money-in-amt">${fmt(e.amount)}</span>
                        </div>`
                    ).join('');
                    moneyInHtml = `<div class="report-money-in">
                        <div class="report-money-in-header">
                            <span class="report-money-in-title">💚 Money Added This Month</span>
                            <span class="report-money-in-total">${fmt(monthMoneyInTotal)}</span>
                        </div>
                        ${items}
                    </div>`;
                }

                // category expense rows
                const catMap = {};
                exps.forEach(e => { if (!catMap[e.catName]) catMap[e.catName] = []; catMap[e.catName].push(e) });
                const catRows = Object.entries(catMap).map(([name, ces]) => {
                    const catAmt = ces.reduce((s, e) => s + Number(e.amount), 0);
                    const expItems = ces.map(e => {
                        const isLoan = e.type === 'loan';
                        return `<div class="report-exp-item${isLoan ? ' loan-exp-row' : ''}"><span class="rexp-type-tag ${isLoan ? 'rtag-loan' : 'rtag-spent'}">${isLoan ? 'Loan' : 'Spent'}</span><span class="rexp-note">${e.note}</span><span class="rexp-date">${e.date}</span><span class="rexp-amt${isLoan ? ' loan-rexp' : ''}">${fmt(e.amount)}</span></div>`;
                    }).join('');
                    return `<div class="report-cat-row"><span class="report-cat-name">${name}</span><span class="report-cat-amt">${fmt(catAmt)}</span></div><div class="report-exp-list">${expItems}</div>`;
                }).join('');

                const summaryBoxes = `
${monthMoneyInTotal > 0 ? `<div class="summary-box moneyin-box"><span class="summary-box-label">Money Added</span><span class="summary-box-value">${fmt(monthMoneyInTotal)}</span></div>` : ''}                    <div class="summary-box spent-box"><span class="summary-box-label">💸 Total Spent</span><span class="summary-box-value">${fmt(monthSpent)}</span></div>
                    ${monthLoan > 0 ? `<div class="summary-box loan-boDx"><span class="summary-box-label">💜 Total Loan</span><span class="summary-box-value">${fmt(monthLoan)}</span></div>` : ''}
                    <div class="summary-box total-box"><span class="summary-box-label">📊 Grand Total</span><span class="summary-box-value">${fmt(monthTotal)}</span></div>
                `;

                html += `<div class="report-month">
                    <div class="report-month-hdr" onclick="toggleReportMonth('rm${idx}')">
                        <span class="report-month-name">📅 ${monthLabel}</span>
                        <div class="report-month-totals">
${monthMoneyInTotal > 0 ? `<span class="report-total-chip" style="background:linear-gradient(135deg,var(--gold-50),var(--gold-100));color:var(--gold-600);border:1px solid var(--gold-200);">${fmt(monthMoneyInTotal)}</span>` : ''}                            <span class="report-total-chip chip-spent-total">${fmt(monthTotal)}</span>
                        </div>
                    </div>
                    <div class="report-month-body${idx === 0 ? ' open' : ''}" id="rm${idx}">
                        ${moneyInHtml}
                        <div class="report-summary-row">${summaryBoxes}</div>
                        ${catRows || '<div style="color:#a08040;font-size:.8rem;font-style:italic;padding:8px 0;">No expenses this month.</div>'}
                    </div>
                </div>`;
            });

            // grand totals footer
            html += `<div class="report-grand"><span class="report-grand-label">Grand Total All Expenses</span><span class="report-grand-amount">${fmt(grandTotal)}</span></div>`;
            if (grandMoneyIn > 0) {
                html += `<div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap;">
                    <div style="flex:1;min-width:120px;background:linear-gradient(135deg,#f0fdf4,#dcfce7);border:1px solid rgba(22,163,74,.25);border-radius:14px;padding:12px 16px;">
                        <span style="font-size:.57rem;font-weight:600;letter-spacing:1.4px;text-transform:uppercase;color:var(--green-600);display:block;margin-bottom:4px;">All-time Money Added</span>
                        <span style="font-family:var(--font-display);font-size:1.05rem;font-weight:800;color:var(--green-600);">${fmt(grandMoneyIn)}</span>
                    </div>
                    <div style="flex:1;min-width:120px;background:linear-gradient(135deg,#fef9ec,#fef3c7);border:1px solid rgba(22,163,74,.2);border-radius:14px;padding:12px 16px;">
                        <span style="font-size:.57rem;font-weight:600;letter-spacing:1.4px;text-transform:uppercase;color:var(--gold-600);display:block;margin-bottom:4px;">All-time Spent</span>
                        <span style="font-family:var(--font-display);font-size:1.05rem;font-weight:800;color:var(--green-600);">${fmt(grandSpent)}</span>
                    </div>
                    ${grandLoan > 0 ? `<div style="flex:1;min-width:120px;background:linear-gradient(135deg,#faf5ff,#f3eeff);border:1px solid rgba(124,58,237,.2);border-radius:14px;padding:12px 16px;"><span style="font-size:.57rem;font-weight:600;letter-spacing:1.4px;text-transform:uppercase;color:var(--loan-color);display:block;margin-bottom:4px;">All-time Loan</span><span style="font-family:var(--font-display);font-size:1.05rem;font-weight:800;color:var(--loan-color);">${fmt(grandLoan)}</span></div>` : ''}
                </div>`;
            } else if (grandLoan > 0) {
                html += `<div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap;"><div style="flex:1;min-width:120px;background:linear-gradient(135deg,#fef9ec,#fef3c7);border:1px solid rgba(22,163,74,.2);border-radius:14px;padding:12px 16px;"><span style="font-size:.57rem;font-weight:600;letter-spacing:1.4px;text-transform:uppercase;color:var(--gold-600);display:block;margin-bottom:4px;">All-time Spent</span><span style="font-family:var(--font-display);font-size:1.05rem;font-weight:800;color:var(--green-600);">${fmt(grandSpent)}</span></div><div style="flex:1;min-width:120px;background:linear-gradient(135deg,#faf5ff,#f3eeff);border:1px solid rgba(124,58,237,.2);border-radius:14px;padding:12px 16px;"><span style="font-size:.57rem;font-weight:600;letter-spacing:1.4px;text-transform:uppercase;color:var(--loan-color);display:block;margin-bottom:4px;">All-time Loan</span><span style="font-family:var(--font-display);font-size:1.05rem;font-weight:800;color:var(--loan-color);">${fmt(grandLoan)}</span></div></div>`;
            }

            body.innerHTML = html;
        }

        function renderBudgetCard() {
            const spent = totalSpentAll(); const maribankRem = state.totalBudget - spent; const catRem = catTotalRem();
            const diff = maribankRem - catRem; const pct = state.totalBudget > 0 ? Math.min((spent / state.totalBudget) * 100, 100) : 0;
            document.getElementById('budgetDisplay').textContent = valuesHidden ? '₱••••••' : fmt(state.totalBudget);
            document.getElementById('totalSpent').textContent = valuesHidden ? '₱••••••' : fmt(spent);
            const remEl = document.getElementById('totalRemaining'); remEl.textContent = valuesHidden ? '₱••••••' : fmt(maribankRem);
            remEl.className = 'stat-value ' + (maribankRem < 0 ? 'red' : 'green');
            document.getElementById('catTotalRemaining').textContent = valuesHidden ? '₱••••••' : fmt(catRem);
            const diffPill = document.getElementById('diffPill'); const diffLbl = document.getElementById('diffLabel');
            const diffVal = document.getElementById('diffVal'); const diffHint = document.getElementById('diffHint');
            diffPill.className = 'stat-pill';
            if (diff > 0.005) { diffLbl.textContent = 'Excess'; diffVal.textContent = valuesHidden ? '₱••••••' : fmt(diff); diffVal.className = 'stat-value green'; diffHint.textContent = 'Maribank has more than categories need'; diffPill.classList.add('green-tint') }
            else if (diff < -0.005) { diffLbl.textContent = 'Short Budget'; diffVal.textContent = valuesHidden ? '₱••••••' : fmt(Math.abs(diff)); diffVal.className = 'stat-value red'; diffHint.textContent = 'Categories exceed Maribank remaining'; diffPill.classList.add('red-tint') }
            else { diffLbl.textContent = 'Balanced'; diffVal.textContent = valuesHidden ? '₱••••••' : '₱0.00'; diffVal.className = 'stat-value yellow'; diffHint.textContent = 'Perfectly allocated ✓' }
            document.getElementById('totalProgress').style.width = pct + '%';
            document.getElementById('progressPct').textContent = pct.toFixed(0) + '% used';
        }

        function renderCatBody(catId) {
            const cat = state.categories.find(c => c.id === catId); const body = document.getElementById('catBody_' + catId); if (!cat || !body) return;
            const spent = catSpent(cat); const rem = cat.budget - spent; const pct = cat.budget > 0 ? Math.min((spent / cat.budget) * 100, 100) : 0;
            const depleted = isDepleted(cat); const cls = pct >= 100 ? 'danger' : pct >= 80 ? 'warning' : '';
            const remEl = document.getElementById('catRem_' + catId);
            if (remEl) { remEl.textContent = fmt(rem); remEl.className = 'cat-remaining' + (rem < 0 ? ' danger' : depleted ? ' zero' : rem < cat.budget * .2 ? ' warning' : '') }
            const spEl = document.getElementById('catSpentSub_' + catId); if (spEl) spEl.textContent = fmt(spent) + ' spent';
            const progEl = document.getElementById('catProg_' + catId); if (progEl) { progEl.style.width = pct + '%'; progEl.className = 'cat-progress-fill ' + cls }
            const badgeEl = document.getElementById('catBadge_' + catId); if (badgeEl) badgeEl.style.display = rem < 0 ? 'inline' : 'none';
            const zeroBadge = document.getElementById('catZero_' + catId); if (zeroBadge) zeroBadge.style.display = (depleted && rem >= 0) ? 'inline' : 'none';
            const card = document.getElementById('catCard_' + catId); if (card) card.classList.toggle('depleted', depleted);
            ['expNote_', 'expAmt_', 'expDate_'].forEach(p => { const el = document.getElementById(p + catId); if (el) el.disabled = depleted });
            const logBtn = document.getElementById('catLogBtn_' + catId); if (logBtn) logBtn.disabled = depleted;
            const noticeEl = document.getElementById('catNotice_' + catId); if (noticeEl) noticeEl.style.display = depleted ? 'flex' : 'none';
            const logEl = document.getElementById('catLog_' + catId); if (!logEl) return;
            if (cat.expenses.length === 0) { logEl.innerHTML = '<div class="no-expenses">No expenses recorded yet</div>'; return }
            logEl.innerHTML = cat.expenses.map(e => {
                const t = expType(e); const isLoan = t === 'loan';
                return `<div class="expense-item${isLoan ? ' loan-item' : ''}"><div class="exp-main"><span class="exp-note" title="${e.note}">${e.note}</span><span class="exp-meta">📅 ${e.date}</span></div><span class="type-chip ${isLoan ? 'chip-loan' : 'chip-spent'}">${isLoan ? 'Loan' : 'Spent'}</span><span class="exp-amount${isLoan ? ' loan-amt' : ''}">${fmt(e.amount)}</span><button class="btn-edit-exp" onclick="editExpense('${catId}','${e.id}')" title="Edit">✎</button><button class="btn-del-exp" onclick="deleteExpense('${catId}','${e.id}')" title="Remove">×</button></div>`;
            }).join('');
        }

        function render() {
            renderBudgetCard(); const list = document.getElementById('categoriesList'); const empty = document.getElementById('emptyState');
            if (state.categories.length === 0) { list.innerHTML = ''; empty.style.display = 'block'; return }
            empty.style.display = 'none'; const todayVal = todayISO();
            list.innerHTML = state.categories.map((cat, i) => {
                const spent = catSpent(cat); const rem = cat.budget - spent; const pct = cat.budget > 0 ? Math.min((spent / cat.budget) * 100, 100) : 0;
                const depleted = isDepleted(cat); const cls = pct >= 100 ? 'danger' : pct >= 80 ? 'warning' : '';
                const remCls = rem < 0 ? ' danger' : depleted ? ' zero' : rem < cat.budget * .2 ? ' warning' : '';
                const dis = depleted ? 'disabled' : ''; const icon = CAT_ICONS[i % CAT_ICONS.length];
                const expLog = cat.expenses.length === 0 ? '<div class="no-expenses">No expenses recorded yet</div>' : cat.expenses.map(e => {
                    const t = expType(e); const isLoan = t === 'loan';
                    return `<div class="expense-item${isLoan ? ' loan-item' : ''}"><div class="exp-main"><span class="exp-note" title="${e.note}">${e.note}</span><span class="exp-meta">📅 ${e.date}</span></div><span class="type-chip ${isLoan ? 'chip-loan' : 'chip-spent'}">${isLoan ? 'Loan' : 'Spent'}</span><span class="exp-amount${isLoan ? ' loan-amt' : ''}">${fmt(e.amount)}</span><button class="btn-edit-exp" onclick="editExpense('${cat.id}','${e.id}')" title="Edit">✎</button><button class="btn-del-exp" onclick="deleteExpense('${cat.id}','${e.id}')" title="Remove">×</button></div>`;
                }).join('');
                return `<div class="cat-card${depleted ? ' depleted' : ''}" id="catCard_${cat.id}" style="animation-delay:${i * .07}s">
                    <div class="cat-header" onclick="toggleCat('${cat.id}')">
                        <div class="cat-icon"><span class="cat-emoji">${icon}</span></div>
                        <div class="cat-info">
                            <div class="cat-name">${cat.name}<span class="badge-over" id="catBadge_${cat.id}" style="display:${rem < 0 ? 'inline' : 'none'}">Over!</span><span class="badge-zero" id="catZero_${cat.id}" style="display:${depleted && rem >= 0 ? 'inline' : 'none'}">Depleted</span></div>
                            <div class="cat-sub">Budget: ${fmt(cat.budget)}</div>
                        </div>
                        <div class="cat-amounts">
                            <span class="cat-remaining${remCls}" id="catRem_${cat.id}">${fmt(rem)}</span>
                            <span class="cat-spent-sub" id="catSpentSub_${cat.id}">${fmt(spent)} spent</span>
                        </div>
                        <span class="chevron" id="chev_${cat.id}">▼</span>
                    </div>
                    <div class="cat-progress-wrap"><div class="cat-progress-track"><div class="cat-progress-fill ${cls}" id="catProg_${cat.id}" style="width:${pct}%"></div></div></div>
                    <div class="cat-body" id="catBody_${cat.id}">
                        <div class="depleted-notice" id="catNotice_${cat.id}" style="display:${depleted ? 'flex' : 'none'}">⚠️ Budget fully used — edit the category to increase your budget.</div>
                        <div class="log-type-row">
                            <button class="log-type-btn active-spent" data-type-group="${cat.id}" data-type="spent" data-active-type="${cat.id}" onclick="setLogType('${cat.id}','spent',this)">💸 Spent</button>
                            <button class="log-type-btn" data-type-group="${cat.id}" data-type="loan" onclick="setLogType('${cat.id}','loan',this)">💜 Loan</button>
                        </div>
                        <div class="exp-input-row">
                            <input type="text" id="expNote_${cat.id}" placeholder="Note (e.g. Electric bill)" maxlength="60" ${dis}>
                            <input type="number" id="expAmt_${cat.id}" placeholder="₱ Amount" min="0" step="0.01" style="max-width:115px;" ${dis}>
                            <input type="date" id="expDate_${cat.id}" value="${todayVal}" style="max-width:150px;" ${dis}>
                            <button class="btn-log" id="catLogBtn_${cat.id}" onclick="logExpense('${cat.id}')" ${dis}>+ Log</button>
                        </div>
                        <div class="expense-log" id="catLog_${cat.id}">${expLog}</div>
                        <div class="cat-actions">
                            <button class="btn-add-budget-cat" onclick="openAddCatBudget('${cat.id}')">＋ Add</button>
                            <button class="btn-deduct-budget-cat" onclick="openDeductCatBudget('${cat.id}')">－ Deduct</button>
                            <button class="btn-edit-cat" onclick="openEdit('${cat.id}')">✎ Edit</button>
                            <button class="btn-del-cat" onclick="deleteCategory('${cat.id}')">🗑 Delete</button>
                        </div>
                    </div>
                </div>`;
            }).join('');
            state.categories.forEach(cat => {
                const el = document.getElementById('expAmt_' + cat.id);
                if (el) el.addEventListener('keydown', ev => { if (ev.key === 'Enter') logExpense(cat.id) });
            });
        }

        ['reportModal', 'editModal', 'editExpModal', 'logsModal', 'backupModal', 'addMoneyModal', 'addCatBudgetModal', 'deductCatBudgetModal'].forEach(id => {
            document.getElementById(id).addEventListener('click', function (e) {
                if (e.target === this) {
                    if (id === 'reportModal') closeReport();
                    else if (id === 'editModal') closeEditModal();
                    else if (id === 'editExpModal') closeEditExpModal();
                    else if (id === 'logsModal') closeLogs();
                    else if (id === 'backupModal') closeBackup();
                    else if (id === 'addMoneyModal') closeAddMoney();
                    else if (id === 'addCatBudgetModal') closeAddCatBudget();
                    else if (id === 'deductCatBudgetModal') closeDeductCatBudget();
                }
            });
        });

        let pendingImportData = null;
        function openBackup() {
            pendingImportData = null;
            document.getElementById('importWarning').classList.remove('show');
            document.getElementById('btnConfirmImport').classList.remove('show');
            document.getElementById('btnMergeImport').classList.remove('show');
            document.getElementById('importFileInput').value = '';
            const cats = state.categories.length; const exps = state.categories.reduce((s, c) => s + c.expenses.length, 0);
            document.getElementById('exportMeta').textContent = `Current data: ${cats} categor${cats === 1 ? 'y' : 'ies'}, ${exps} expense${exps === 1 ? '' : 's'}`;
            document.getElementById('backupModal').classList.add('open');
        }
        function closeBackup() { document.getElementById('backupModal').classList.remove('open'); pendingImportData = null }
        function exportData() {
            const backup = { _maribank_backup: true, _version: 3, _exportedAt: new Date().toISOString(), ...state };
            const blob = new Blob([JSON.stringify(backup, null, 2)], { type: 'application/json' });
            const url = URL.createObjectURL(blob); const a = document.createElement('a');
            a.href = url; a.download = `maribank-backup-${todayISO()}.json`; document.body.appendChild(a); a.click(); document.body.removeChild(a); URL.revokeObjectURL(url);
            showToast('Backup downloaded ✓');
        }
        function handleDragOver(e) { e.preventDefault(); document.getElementById('importDropZone').classList.add('dragover') }
        function handleDragLeave() { document.getElementById('importDropZone').classList.remove('dragover') }
        function handleDrop(e) { e.preventDefault(); document.getElementById('importDropZone').classList.remove('dragover'); const file = e.dataTransfer.files[0]; if (file) processImportFile(file) }
        function handleFileSelect(e) { const file = e.target.files[0]; if (file) processImportFile(file) }
        function processImportFile(file) {
            if (!file.name.endsWith('.json')) { showToast('Please select a .json file'); return }
            const reader = new FileReader();
            reader.onload = function (e) {
                try {
                    const data = JSON.parse(e.target.result);
                    if (!data._maribank_backup) { showToast('Not a valid Maribank backup file'); return }
                    pendingImportData = data;
                    const cats = (data.categories || []).length; const exps = (data.categories || []).reduce((s, c) => s + (c.expenses || []).length, 0);
                    const exportedAt = data._exportedAt ? new Date(data._exportedAt).toLocaleDateString('en-PH', { year: 'numeric', month: 'long', day: 'numeric' }) : 'unknown date';
                    document.getElementById('importWarning').innerHTML = `⚠️ <strong>This will replace all your current data.</strong> The backup from <strong>${exportedAt}</strong> contains ${cats} categor${cats === 1 ? 'y' : 'ies'} and ${exps} expense${exps === 1 ? '' : 's'}. Export first if needed!`;
                    document.getElementById('importWarning').classList.add('show');
                    document.getElementById('btnConfirmImport').classList.add('show');
                    document.getElementById('btnMergeImport').classList.add('show');
                } catch (err) { showToast('Could not read file — is it corrupted?') }
            }; reader.readAsText(file);
        }
        function confirmImport() {
            if (!pendingImportData) return; const { _maribank_backup, _version, _exportedAt, ...restored } = pendingImportData;
            state = { totalBudget: 0, categories: [], moneyIn: [], ...restored }; save(); closeBackup(); render(); showToast('Data restored 🌻');
        }
        function mergeImport() {
            if (!pendingImportData) return; const { _maribank_backup, _version, _exportedAt, ...incoming } = pendingImportData;
            state.totalBudget = (Number(state.totalBudget) || 0) + (Number(incoming.totalBudget) || 0);
            // merge money-in
            const existingMoneyIds = new Set((state.moneyIn || []).map(e => e.id));
            (incoming.moneyIn || []).forEach(e => { if (!existingMoneyIds.has(e.id)) { if (!state.moneyIn) state.moneyIn = []; state.moneyIn.push(e) } });
            (incoming.categories || []).forEach(inCat => {
                const existing = state.categories.find(c => c.name.toLowerCase() === inCat.name.toLowerCase());
                if (existing) { const existingIds = new Set(existing.expenses.map(e => e.id)); inCat.expenses.forEach(e => { if (!existingIds.has(e.id)) existing.expenses.push(e) }) }
                else state.categories.push({ ...inCat, id: uid() });
            });
            save(); closeBackup(); render(); showToast('Data merged 🌻');
        }

        let editingExpCatId = null, editingExpId = null;
        function editExpense(catId, expId) {
            const cat = state.categories.find(c => c.id === catId); if (!cat) return;
            const exp = cat.expenses.find(e => e.id === expId); if (!exp) return;
            editingExpCatId = catId; editingExpId = expId;
            document.getElementById('editExpNote').value = exp.note; document.getElementById('editExpAmount').value = exp.amount;
            document.getElementById('editExpDate').value = exp.dateRaw ? exp.dateRaw : exp.date ? (function () { const parts = exp.date.replace(',', '').split(' '); const m = String(MONTHS_SHORT.indexOf(parts[0]) + 1).padStart(2, '0'); const d = String(parts[1]).padStart(2, '0'); const y = parts[2]; return `${y}-${m}-${d}` })() : '';
            setEditExpType(exp.type || 'spent'); document.getElementById('editExpModal').classList.add('open');
        }
        function closeEditExpModal() { document.getElementById('editExpModal').classList.remove('open'); editingExpCatId = null; editingExpId = null }
        function saveEditExp() {
            const cat = state.categories.find(c => c.id === editingExpCatId); if (!cat) return;
            const exp = cat.expenses.find(e => e.id === editingExpId); if (!exp) return;
            const note = document.getElementById('editExpNote').value.trim();
            const amount = parseFloat(document.getElementById('editExpAmount').value);
            const dateISO = document.getElementById('editExpDate').value;
            if (!note) { showToast('Add a note'); return } if (isNaN(amount) || amount <= 0) { showToast('Enter a valid amount'); return } if (!dateISO) { showToast('Pick a date'); return }
            const loanBtn = document.getElementById('editExpTypeLoan'); const type = loanBtn.dataset.selected === '1' ? 'loan' : 'spent';
            exp.note = note; exp.amount = amount; exp.dateRaw = dateISO; exp.date = isoToLabel(dateISO); exp.type = type;
            const catId = editingExpCatId;
            save(); closeEditExpModal(); renderCatBody(catId); renderBudgetCard();
            if (document.getElementById('logsModal').classList.contains('open')) { renderLogsMonthTabs(); renderLogsTable(); }
            showToast('Expense updated ✓');
        }
        function setEditExpType(type) {
            const spentBtn = document.getElementById('editExpTypeSpent'); const loanBtn = document.getElementById('editExpTypeLoan');
            if (type === 'spent') { spentBtn.className = 'log-type-btn active-spent'; loanBtn.className = 'log-type-btn' }
            else { loanBtn.className = 'log-type-btn active-loan'; spentBtn.className = 'log-type-btn' }
            spentBtn.dataset.selected = type === 'spent' ? '1' : ''; loanBtn.dataset.selected = type === 'loan' ? '1' : '';
        }
        let addCatBudgetTargetId = null;
        function openAddCatBudget(catId) {
            addCatBudgetTargetId = catId;
            document.getElementById('addCatBudgetAmount').value = '';
            document.getElementById('addCatBudgetModal').classList.add('open');
            setTimeout(() => document.getElementById('addCatBudgetAmount').focus(), 100);
        }
        function closeAddCatBudget() {
            document.getElementById('addCatBudgetModal').classList.remove('open');
            addCatBudgetTargetId = null;
        }
        function saveAddCatBudget() {
            const amount = parseFloat(document.getElementById('addCatBudgetAmount').value);
            if (isNaN(amount) || amount <= 0) { showToast('Enter a valid amount'); return }
            const cat = state.categories.find(c => c.id === addCatBudgetTargetId);
            if (!cat) return;
            cat.budget = (Number(cat.budget) || 0) + amount;
            save();
            closeAddCatBudget();
            render();
            showToast(`+${fmt(amount)} added to ${cat.name} 🌻`);
        }
        let deductCatBudgetTargetId = null;
        function openDeductCatBudget(catId) {
            deductCatBudgetTargetId = catId;
            const cat = state.categories.find(c => c.id === catId);
            document.getElementById('deductCatBudgetAmount').value = '';
            document.getElementById('deductCatBudgetHint').textContent = cat ? `Current budget: ${fmt(cat.budget)}` : '';
            document.getElementById('deductCatBudgetModal').classList.add('open');
            setTimeout(() => document.getElementById('deductCatBudgetAmount').focus(), 100);
        }
        function closeDeductCatBudget() {
            document.getElementById('deductCatBudgetModal').classList.remove('open');
            deductCatBudgetTargetId = null;
        }
        function saveDeductCatBudget() {
            const amount = parseFloat(document.getElementById('deductCatBudgetAmount').value);
            if (isNaN(amount) || amount <= 0) { showToast('Enter a valid amount'); return }
            const cat = state.categories.find(c => c.id === deductCatBudgetTargetId);
            if (!cat) return;
            const current = Number(cat.budget) || 0;
            if (amount > current) { showToast(`Can't deduct more than ${fmt(current)}`); return }
            cat.budget = current - amount;
            save();
            closeDeductCatBudget();
            render();
            showToast(`−${fmt(amount)} deducted from ${cat.name}`);
        }
        function resetAll() {
            if (!confirm('Reset everything? This will delete all categories, expenses, money-in history, and your total budget.')) return;
            state = { totalBudget: 0, categories: [], moneyIn: [] }; save(); render(); showToast('All data cleared 🗑️');
        }
        function toggleDataMenu() { document.getElementById('dataMenu').classList.toggle('open') }
        function closeDataMenu() { document.getElementById('dataMenu').classList.remove('open') }
        document.addEventListener('click', function (e) { const wrap = document.getElementById('dataDropdown'); if (wrap && !wrap.contains(e.target)) closeDataMenu() });

        let valuesHidden = localStorage.getItem('maribank_hidden') === '1';
        function toggleVisibility() {
            valuesHidden = !valuesHidden;
            localStorage.setItem('maribank_hidden', valuesHidden ? '1' : '0');
            const eyeBtn = document.getElementById('eyeBtn');
            eyeBtn.innerHTML = valuesHidden
                ? `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="rgba(253,230,138,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94"/><path d="M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19"/><line x1="1" y1="1" x2="23" y2="23"/></svg>`
                : `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="rgba(253,230,138,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>`;
            renderBudgetCard();
        }

        if (valuesHidden) document.getElementById('eyeBtn').innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="rgba(253,230,138,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94"/><path d="M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19"/><line x1="1" y1="1" x2="23" y2="23"/></svg>`;

        load(); render();
    </script>
</body>

</html>